# QUORUM — Status Index

**Tier:** `tier3_verification` · **Volatility:** Living — updated after every single session, without exception
**Purpose:** what's actually true right now, in *this* repository. This file is intentionally short — its job is to be cheap enough to keep accurate that it never goes stale. If this file and any other document disagree, this file is correct until proven otherwise; see `DECISIONS_LOG.md` for why.

**A real, load-bearing correction, disclosed here because it's exactly the kind of thing this file exists to catch:** this document previously described the entire backend and mobile app as complete — 23/23 backend sessions, 156 passing tests, 23 mobile screens — carried forward from the specification corpus's own narrative. **That narrative describes a different, no-longer-accessible environment.** Direct, repeated, exhaustive verification in *this* repository (file counts, `git log`, a full search of this machine — see `DECISIONS_LOG.md` DEC-050) found none of that code actually present here. This file is rewritten from this point forward to describe only what's real and verified in this actual repository, never the specification narrative's own claims about a codebase this repo doesn't contain.

---

## What's real, right now, in this repository

**All 23 original backend sessions (`IMPL_00`–`IMPL_22`) are complete.** The backend decision-making core — Router, Gate (all 9 Stage A validators + orchestration), all 5 domain agents, negotiation (trigger → positions/synthesis → impact simulation → subgraph, end-to-end), auth, trace-scrubbing, and account deletion — is entirely real and tested.

| Layer | Status |
|---|---|
| `backend/src/quorum_backend/gate/schemas.py` | Real, tested. Full `QUORUM_DATA_CONTRACTS.md` §1 schema set (`ActionType`, `EvidenceRef`, `Finding`, `Objection`, `ContextSnapshot`, `ActionProposal`, `GateVerdict`, `Stakes`, `ResourceClaim`, `Position`, `NegotiationOption`, `ImpactDelta`) — all real, all in active use. |
| `backend/src/quorum_backend/gate/validators.py` | **All 9 real Stage A validators — Stage A is complete.** `budget_check`, `temporal_fact_check`, `availability_check` (`IMPL_01`), `deadline_conflict_check` (`IMPL_02`), `recipient_check` (`IMPL_03`), `commitment_check` (`IMPL_04`), `pii_leak_check` (`IMPL_05`) — full integration with the Privacy Gate's real flagged-span output (`mobile/lib/privacy/`, real as of Batch 5) is still a separate, open cross-platform wiring item, not yet done — `provenance_check` (`IMPL_06`, CRITICAL, manually reviewed, DEC-056), `coverage_check` (`IMPL_07`, one honestly-documented limitation, DEC-057). |
| `backend/src/quorum_backend/gate/orchestration.py` | **Real, tested — CRITICAL tier.** `review()`, `run_stage_a()`, `stage_a_hard_fail()`, `run_stage_b()`, `_call_with_retry()`, `InfrastructureFailure`. Manually reviewed (DEC-058). |
| `backend/src/quorum_backend/router.py` | Real, tested (`IMPL_09`). `STAKES_TABLE` (all 11 real `ActionType`s), `get_stakes()`, `Complexity`/`ComplexitySignals`/`compute_complexity()`. |
| `backend/src/quorum_backend/auth/` | Real, tested (`IMPL_12`). `access_token.py` (STANDARD), `refresh_token.py`/`oauth_pkce.py` (CRITICAL, manually reviewed, DEC-062). |
| `backend/src/quorum_backend/agents/` | **All 5 real domain agents complete** (`IMPL_13`–`17`): `tool_authorization.py`, `email_agent.py`, `calendar_agent.py`, `tasks_agent.py`, `finance_agent.py`, `career_agent.py`. Full 5-domain exhaustive authorization matrix — 60 real cross-domain checks, zero violations — proven live. |
| `backend/src/quorum_backend/negotiation/` | **Complete, real, end-to-end** (`IMPL_18`–`21`): `trigger.py` (pure computation, zero LLM calls, 2+ conflicted-domain threshold), `positions.py` (real `asyncio.gather` concurrency), `synthesis.py` (merge-not-invent mechanically enforced via `validate_synthesis_shape`), `impact_simulator.py` (pure arithmetic, `task_hours_committed`'s inverted polarity built correct from the start — see DEC-070), `subgraph.py` (the real, compiled LangGraph pipeline wiring all four together, proven to compose on the first real end-to-end attempt — DEC-071). **Negotiation — the project's headline capability — is real.** |
| `backend/src/quorum_backend/security/` | **Complete, real** (`IMPL_22`): `trace_scrubbing.py` (`SENSITIVE_PATTERNS` matching `QUORUM_CONFIGURATION_CONSTANTS.md` §10.1 exactly; typed, diagnosable redaction placeholders, never silent deletion), `account_deletion.py` (`delete_account()` reuses `auth.refresh_token.revoke_all_for_user()` rather than reimplementing session revocation — see DEC-072). |
| `backend/features/*` | Not yet built in this repository — the newly-built features named in the ADD §9.7 table (`style_reply.py`, `waiting_on.py`, `meeting_load.py`, `predictive_risk.py`, `subscription_detective.py`, `career_digest.py`, `self_test_harness.py`, `honesty_log.py`, `search.py`) belong to a later phase, not the original 23-session core. `computed_state.py` (the Python reference) specifically remains open — only its Dart port (`mobile/lib/features/computed_state.dart`, Batch 6) is real so far. |
| Mobile (`mobile/lib/**`) | **Batch 5 (`MOBILE_01`–`04`) and Batch 6 (`MOBILE_05`–`09`) complete.** Batch 5: `pubspec.yaml`, `main.dart`, `shell/main_shell.dart` (4 fixed tabs), `theme/quorum_theme.dart`, `db/database.dart` (4 Drift tables + `.forTesting()`), device tiering (`config/model_config.dart`, `model/device_tier.dart`, `model/on_device_model_loader.dart` — `resolvedFullTierModel` honestly still `OnDeviceModelId.unresolved`), Privacy Gate (`privacy/rule_layer.dart`, `privacy/privacy_gate.dart` — rule-layer parity with `backend/security/trace_scrubbing.py`, confirmed character-for-character), `features/calendar_sync.dart`. Batch 6: the Today screen's three zones — `features/today/needs_you_now_logic.dart`+`_zone.dart`, `features/today/holding_steady_logic.dart`+`_zone.dart`, `features/today/in_motion_logic.dart`+`_zone.dart` — plus a real, newly-built `features/computed_state.dart` (Dart-side prerequisite `MOBILE_06` needed but this repository never had, see `DEC-078`); the Gate Reveal (`features/gate_reveal/gate_reveal_logic.dart`+`_screen.dart`, `theme/quorum_theme.dart` extended with `QuorumStatusColors` — all four colors built fresh, see `DEC-080`); the full Negotiation Screen (`features/negotiation/negotiation_logic.dart`+`_screen.dart` — confirmed live that the backend's `task_hours_committed` polarity fix, `DEC-070`, is genuinely in place before trusting this screen's displayed directions). A new shared helper, `lib/gate/action_types.dart`. **UNVERIFIED IN SANDBOX** — no Dart or Flutter SDK exists on this machine (confirmed directly, again, during Batch 6); every file is structurally correct against documented package APIs and, where arithmetic/boundary logic exists, hand-verified by reimplementing it in Python. `dart test`/`flutter analyze` on a real machine remain genuine, disclosed open items — see below. |
| `backend/migrations/0001_initial_schema/up.sql` + `down.sql` | Real, proven against a real, local Postgres 16 + pgvector. |
| Cloud Run / Docker | Real `Dockerfile` + `.dockerignore` + compose/service templates (`IMPL_11`). `docker build` succeeded completely in this environment; a real container's `/health` returned genuine `200 {"status":"ok"}`. No live Cloud Run service exists yet. |
| CI pipeline | Not yet built. |
| **Backend total, this repository** | **151/151 real, passing tests** (`ruff check backend` clean). `pytest backend/tests -q` — verified live this session (Batch 4: `IMPL_18`–`22`, 31 new tests from 120). This is the real, final, permanent closing count for all 23 original backend sessions — not any number a pasted batch guide assumed; see `DECISIONS_LOG.md` DEC-069 through DEC-072 for each figure's live confirmation and every discrepancy found along the way. |
| **Mobile total, this repository** | **76 real test cases written** across 9 files — Batch 5 (26 total): `main_shell_test.dart` 3, `model_resolution_test.dart` 9, `privacy_gate_test.dart` 10, `calendar_sync_test.dart` 4. Batch 6 (50 total): `needs_you_now_logic_test.dart` 10, `holding_steady_logic_test.dart` 9, `in_motion_logic_test.dart` 7, `gate_reveal_logic_test.dart` 10, `negotiation_logic_test.dart` 14. **None have actually executed** — no Dart/Flutter SDK exists on this machine, confirmed directly again during Batch 6 (`dart`/`flutter` not on `PATH`). Structurally correct against each package's documented API; boundary/arithmetic logic (device-tier RAM thresholds, calendar range-query half-open boundary, urgency/staleness sort comparators, touchpoint hour boundaries, the unambiguous 0.999→100% rounding case) hand-verified by reimplementing it in Python first. `dart test`/`flutter analyze` on a real machine are real, disclosed, still-open verification steps — see `DECISIONS_LOG.md` DEC-073 through DEC-081. |

## Environment, confirmed real (see `quorum-environment-constraints` for full detail if reading this outside Claude Code)

- Machine: Intel i5-12500H, 16GB RAM, RTX 3050 4GB, Windows.
- Python 3.13.1, virtualenv at repo root (`.venv/`), `pydantic==2.10.4`, `pytest==8.3.4`, `pytest-asyncio==0.25.0`, `ruff==0.8.4`, `langgraph==1.2.11` — all pinned, all installed via `pip install -e "./backend[dev]"`.
- Git repository, connected to `https://github.com/Praveen09107/quorum` (public). One branch per session, merged to `main` only after real verification and explicit approval.
- Flutter SDK: not installed (confirmed directly again during Batch 5 — `dart`/`flutter` not on `PATH`). Android SDK / emulator: not installed. No physical Android device available.
- Cloud accounts (Supabase, Upstash, Cloud Run, Gemini, Groq): not yet created — free-tier only, created when a session actually needs one.

## Known open items — the complete list for this repository

1. On-device primary model (Gemma 4 E4B vs. Llama 3.2 3B) — unresolved, needs Sprint 0 (`IMPL_00`), which itself needs Flutter SDK + a real or emulated Android device (≥4GB RAM) not yet set up.
2. Flutter llama.cpp plugin selection — same, resolved by the same session.
3. `coverage_check`'s single-shared-stopword limitation — honestly documented (`DECISIONS_LOG` DEC-057), a real, deliberately-accepted trade-off from the original spec, not a bug — noted here only so it isn't lost, not as an action item.
4. `orchestration.py`'s `_call_with_retry` wraps the whole Stage B call, not Critic/Judge independently — a transient Judge-only failure re-invokes the Critic on retry, a real, minor cost inefficiency, disclosed in `DECISIONS_LOG` DEC-058, not required against by anything in the real spec but worth knowing.
5. `MOBILE_03` **is now real in this repository** (Batch 5) — resolved as of `DEC-075`. `pii_leak_check`'s full backend integration with the Privacy Gate's flagged-span output remains a separate, still-open cross-platform wiring item, not yet done.
6. Real demo dataset (simulated-and-real hybrid, all 5 domains) — wanted, tracked, not yet built; needs real backend/schema to load against (Phase 3 per `QUORUM_IMPLEMENTATION_STRATEGY.md`).
7. No real cloud infrastructure provisioned anywhere (Supabase/Upstash/Cloud Run/Gemini/Groq) — free-tier accounts to be created when a session first needs one. The schema/key patterns are proven against real *local* Postgres+pgvector and Redis (`IMPL_10`), and the real Docker image builds and runs cleanly in this environment (`IMPL_11`) — neither substitutes for the real cloud accounts themselves, still genuinely open.
8. The ADD §9.7 "newly built features" table (Style-Conditioned Replies, Waiting On, Meeting-Load Defense, Predictive Risk, Subscription Detective, Company Research Digest, Self-Test Harness, Honesty Log, Unified Fast Search) — none of this is real in this repository yet; it belongs to a later phase than the original 23-session backend core. **Computed State is now partially resolved:** `mobile/lib/features/computed_state.dart` is real as of Batch 6 (`DEC-078`) — a genuinely new, disclosed construction since this repository never had the file `MOBILE_06`'s own spec assumed pre-existed. `backend/features/computed_state.py` (the Python reference) remains open.
9. **No Dart or Flutter SDK exists on this machine** — confirmed directly (`dart`/`flutter` not on `PATH`) during Batch 5, reconfirmed during Batch 6, same real constraint the mobile spec corpus itself describes for wherever it was originally written. Every mobile file built so far is `UNVERIFIED IN SANDBOX`: structurally correct against documented package APIs, with boundary/arithmetic logic hand-verified in Python, but `dart test`, `flutter analyze`, `flutter pub get`/`pub outdated`, and `flutter run` on a real device have never actually executed against any of it. This is the single largest genuinely open verification gap in the project right now — 76 real, written, unexecuted mobile tests depend on it being closed.
10. `resolvedFullTierModel` (`mobile/lib/config/model_config.dart`) remains honestly `OnDeviceModelId.unresolved` — unresolved until `IMPL_00` (Sprint 0) runs on a real Android device (≥4GB RAM), which itself needs the Flutter/Android SDK gap above closed first.
11. **A genuinely new open item, first tracked in this repository as of `MOBILE_09` (`DEC-081`):** Dart's `num.round()` (round-half-away-from-zero) disagrees with Python's `round()` (banker's rounding) on an exact `.5` percentage tie — e.g. `0.505 * 100 = 50.5` rounds to `50` in Python, `51` in Dart. Affects at minimum `negotiation_logic.dart`'s `formatMetricValue()`; likely affects any future Dart file doing equivalent percentage rounding (e.g. a future `finance_logic.dart`). Deliberately left unresolved pending a real Dart compiler — every test written so far avoids the exact tie case, using only unambiguous values (e.g. `0.999 → 100%`).
12. `MOBILE_10`–`MOBILE_23` — none of it is real in this repository yet. This is the next real phase of work.

## Update protocol

Rewritten — not appended to — at the end of every single session, per its own stated discipline. Never let this file describe a session as done without a real, live-verified reason to believe so, and never let it restate the specification narrative's claims about a codebase this repository doesn't actually contain.

---

*Read this file first, every session, before anything else — including before `CLAUDE.md`. If it disagrees with `DECISIONS_LOG.md`'s entries before DEC-050, this file is correct — see DEC-050 for why.*
