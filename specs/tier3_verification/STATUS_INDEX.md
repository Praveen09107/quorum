# QUORUM — Status Index

**Tier:** `tier3_verification` · **Volatility:** Living — updated after every single session, without exception
**Purpose:** what's actually true right now, in *this* repository. This file is intentionally short — its job is to be cheap enough to keep accurate that it never goes stale. If this file and any other document disagree, this file is correct until proven otherwise; see `DECISIONS_LOG.md` for why.

**A real, load-bearing correction, disclosed here because it's exactly the kind of thing this file exists to catch:** this document previously described the entire backend and mobile app as complete — 23/23 backend sessions, 156 passing tests, 23 mobile screens — carried forward from the specification corpus's own narrative. **That narrative describes a different, no-longer-accessible environment.** Direct, repeated, exhaustive verification in *this* repository (file counts, `git log`, a full search of this machine — see `DECISIONS_LOG.md` DEC-050) found none of that code actually present here. This file is rewritten from this point forward to describe only what's real and verified in this actual repository, never the specification narrative's own claims about a codebase this repo doesn't contain.

---

## Product Reality — what a real, signed-in user can actually see and do, right now

**Rewritten (not appended to) at every whole-system checkpoint (`.claude/CLAUDE.md`).** This section answers a genuinely different question than the rest of this file: not "is this code real and tested" (the rest of this file already answers that well) but **"what does a real person actually experience, end to end, today."** Added after a full repository diagnosis found the two questions had quietly diverged for 9 consecutive real sessions (`DEC-120`–`DEC-128`) — real, tested backend work accumulated with nobody checking whether it changed what a user could see or do. See `specs/tier1_foundation/QUORUM_PRODUCTION_COMPLETION_PLAN.md` for the plan closing this gap.

**Genuinely rewritten, not appended to, at this whole-system checkpoint** (`DEC-158`–`DEC-161` in scope, four sessions since the last full rewrite at `DEC-157` — past this section's own "every 2-3 sessions" limit, a real, disclosed miss rather than a silent one; the stretch ran longer than intended because `DEC-159`/`160`/`161` turned into a fast-moving, load-bearing infrastructure chain (a found security gap, the CI/CD pipeline it unblocked, and a real incident on that pipeline's own first run) that made pausing for a checkpoint mid-chain feel like worse discipline than finishing it first). Full historical detail for every claim below lives in `DECISIONS_LOG.md`; this section states only what's true right now and where it was last actually witnessed.

**Nothing NEW is user-doable this checkpoint that wasn't already doable at the last one.** `DEC-158` (Phase 8 Session 4) is more real visual/motion work, entirely mobile, zero backend touch, zero new capability — the same design-system tokens applied to Calendar/Quick-capture/Search/Honesty Log/You, plus this app's first real animation (a timed Gate Stage B reveal, a negotiation-choice cross-fade, an animated capacity/budget number). `DEC-159` through `DEC-161` are entirely backend infrastructure and CI/CD — a real production secrets-storage fix, a real automated build→deploy pipeline, and a real incident-and-fix on that pipeline's own first run — none of which change what a signed-in user can see or do; they change how reliably and safely future changes reach that user.

**Can currently do, last confirmed live on a real device (Realme RMX3853, `2026-08-29`):** sign in with a real Google account and land straight in a persisted session; Today (real capacity/budget numbers, a real "Send an email" S3 card in Needs You Now, a real "Finance vs. Tasks" negotiation In Motion); "Holding steady → View tasks" into the real, live Predictive Risk banner; Trust → "View weekly trend" into the real, correctly per-user `/trust_digest`; You tab's Career Pipeline/Subscriptions/Waiting On/Search/Calendar links, Sign out, and account-deletion UI; Calendar (tapping it triggers a genuine Android permission dialog, granting it renders real synced events). Gate Reveal and the Log tab's Honesty Log were confirmed live at an earlier checkpoint, not re-tapped since. **None of the above has been re-witnessed since Phase 8's new visual system (`DEC-155`–`158`) landed on top of it, across four consecutive sessions now** — see below.

**Real, but NOT yet witnessed on any real device -- the real device has now been disconnected for SEVEN consecutive sessions (`DEC-153` through `DEC-158`... and still, as of this checkpoint), confirmed via `adb devices -l` at the start of every one of them, including this one:**
- **Quick-capture** — a floating action button on every tab, the first real write path beyond negotiation-choice/account-deletion. CRITICAL-tier reviewed, deployed live. Only `tasks` is wired; extending to other domains is a real, logged, deliberately-deferred item.
- **Meeting-Load Defense** — a real 7-day capacity projection banner on Calendar, computed entirely on-device.
- **Phase 8's entire new visual system** (`DEC-155`–`158`) — real typography (IBM Plex Sans/Mono), a considered color/spacing system, real componentry (icon badges, tinted chips), and now real motion (a timed Gate reveal, cross-fades, an animated capacity number) across essentially every screen in the app. `flutter analyze`/`flutter test` confirm it all compiles and every existing behavioral test still passes -- but no human has yet seen any of it actually render, animate, or feel right on a real screen. This is now the single most pressing item for the next real device reconnection, not a formality: four sessions of visual and motion work sight-unseen is a real, growing risk that Phase 8's own final QA session (Session 5) is specifically there to catch, but only once it can actually run.

**`DEC-151`'s `CREATE_CALENDAR_EVENT_EXTERNAL` execution** is real and live-verified but has no real caller yet. Company Research Digest also has no on-device confirmation.

**Still open, not re-diagnosed this checkpoint:** the "Finance vs. Tasks" negotiation's own detail screen still renders with empty positions/options; no autonomously-created negotiation has yet been observed appearing on Today from the real, live trigger jobs against this one real account's current data.

**A real, deliberate non-build decision, not a gap:** the server-side `CalendarAdapter` Protocol stays unimplemented — building a real bridge would mean pushing on-device calendar contents to the cloud, a genuine privacy decision logged as its own open item.

**Cannot currently do, concretely:** edit or delete an existing task/expense/application/event anywhere in the app, or create anything outside the `tasks` domain via free text; have Quorum genuinely send/archive/label a real email or book a real external calendar event on their behalf (all execution paths are real and live-verified, but no real caller exists for any of them yet); reach Memory Transparency from the You tab (blocked on a real `mem0` signup).

**Domains real at the data layer:** Tasks, Finance, Career, Email-ingestion-only (4 of 5). Calendar has no real table at all — real *local*-event ground truth is deliberately on-device instead.

**Real, live infrastructure, materially changed this checkpoint, not just re-confirmed:** the three autonomous cron jobs and encrypted OAuth token handling are unchanged and still live-verified. What's new: **production secrets are no longer stored as plain env vars** (`DEC-159` migrated the 6 highest-blast-radius real credentials to Secret Manager, live-verified byte-exact and functioning under real, unattended cron traffic through the cutover) — a real, previously-undisclosed security gap found incidentally and closed, not a planned improvement. **A real, automated CI/CD pipeline now exists and has been proven under fire, not just written** (`DEC-160`): `ci.yml` builds and deploys to production automatically on every merge to `main`, gated behind six real, narrowly-scoped IAM permissions discovered by live-testing (not guessed), a ref-scoped WIF binding (IAM-enforced, not just YAML-trusted), and a concurrency guard. Its own first-ever real trigger surfaced a genuine production incident within minutes — a stale `--no-traffic` side effect silently pinned live traffic to an old revision while the pipeline itself reported green — found by watching the run closely rather than trusting the checkmark, fixed live immediately, and hardened structurally (`DEC-161`) so the next real deploy (which already happened, and was independently reconfirmed correct) can't silently repeat it. Confirmed live, right now: `GET /health` → real `200`; the live service's traffic is genuinely `latestRevision: true`, resolving to the exact revision the real, automated pipeline most recently built.

**The one concrete next step:** reconnect the real device. Four real, disclosed things are now waiting on it at once -- Quick-capture, Meeting-Load Defense, Phase 8's entire visual system, and its new real motion -- and all four should be checked together in the same real session rather than let the backlog grow further. Separately, and not blocking that: Phase 9-B (credential rotation) remains Preethish's own action across provider consoles, and Phase 9-C (a multi-day autonomous-job observation) accumulates passively regardless of what session runs next.

**On-device:** **resolved for real this session, via a complete real head-to-head comparison (`DEC-130`/`DEC-131`).** Both real Full-tier candidates genuinely downloaded, loaded, and ran real on-device inference on a real physical device — Gemma 4 E4B: 17% validity, 0.1 tok/s; Llama 3.2 3B: 67% validity, 0.8 tok/s. Llama 3.2 3B wins decisively on real, measured accuracy and speed. `mobile/lib/config/model_config.dart`'s `resolvedFullTierModel` is genuinely `OnDeviceModelId.llama32_3B`. Not yet wired into any real user-facing flow — `resolveModelForTier` still has zero real callers anywhere in `mobile/lib/`.

---

## What's real, right now, in this repository

**ALL 46 ORIGINAL BACKEND AND MOBILE SESSIONS ARE COMPLETE** (`IMPL_00`–`22`, `MOBILE_01`–`23`, plus two genuinely new sessions — `MOBILE_22` Screen Composition and the Tasks session — found during execution and given their own full sessions rather than folded into others; see `DEC-096`). The backend decision-making core — Router, Gate (all 9 Stage A validators + orchestration), all 5 domain agents, negotiation (trigger → positions/synthesis → impact simulation → subgraph, end-to-end), auth, trace-scrubbing, and account deletion — is entirely real and tested. **`flutter analyze` and `flutter test` have now genuinely run for the first time in this project's history (`DEC-103`)** — zero analyzer issues, **289/289 real tests passing** as of `DEC-109` — substantially closing Phase 5's own stated gate. Real bugs found and fixed in the process (test-authoring gaps; in `DEC-105`, a genuine layout overflow and a genuine `setState`/`Future` misuse; in `DEC-106`, a genuine, pre-existing, time-of-day-dependent content collision — none production bugs left unfixed). A real, live mobile login screen now exists (`DEC-105`: Google Sign-In, PKCE, secure token storage, proactive refresh) — the real platform scaffolding (`android/`/`ios/`) that had never existed anywhere in this repository was found and backfilled the same session. **The Trust, Career Pipeline, and Finance screens are now genuinely reachable in the real running app** (`DEC-106`, `DEC-108`, `DEC-109`) — `GET /trust` closed both a missing-endpoint gap and a previously-undiscovered wiring gap (`/trust_digest` was real and tested since `DEC-103`/`DEC-105` but never actually reachable on screen until the Trust tab's own primary fetcher existed); `GET /career_pipeline` and `GET /finance/subscriptions` are Part C-2 items that unlock a brand-new screen outright (the You tab's "More" section surfaces each unconditionally once wired, unlike Today/Trust's own gating). A real Flutter/Dart SDK and hardware-accelerated emulator exist on this machine (Batch 10, open item 9). **Sprint 0's real, final benchmark result is in:** both candidate models (Gemma 4 E4B, Llama 3.2 3B) genuinely failed to load on the real emulator, confirmed via `LlamaException: Failed to download ... (SocketException: Failed host lookup: 'huggingface.co' ...)` — the recurring emulator-level DNS relay failure was the real, root cause the whole time, not a plugin hang. A legitimate, spec-anticipated outcome per `IMPL_00`'s own decision rule (`decideWinner()`'s real `StateError` escalation), not a bug — SmolLM2-1.7B becomes the real, honest fallback primary. **A real, blocking correctness gap found while scoping `DELETE /account`, now closed (`DEC-110`):** no user-identity-provisioning system existed anywhere in this backend — a real `users` table and `auth/user_provisioning.py` now exist, and `/tasks`/`/career_pipeline`/`/finance/subscriptions` are genuinely per-user scoped for the first time, with explicit cross-user-isolation tests proving it. **`DELETE /account` itself is now real and live (`DEC-113`)** — a real, atomic, single-transaction purge across every real per-user table, `purge_memories`/`revoke_oauth_tokens` honest disclosed zeros (no `mem0` integration, no persisted Google tokens). **The main app now genuinely builds, installs, and runs on real hardware (`DEC-117`), and a real, human-completed Google sign-in has actually happened (`DEC-118`), closing Roadmap Phase 2 completely.** `DEC-118` found and fixed a real, CRITICAL-tier bug that had silently broken every sign-in attempt since the login screen was first built: the OAuth callback's custom URL scheme reused the real Android `applicationId` (`com.quorum.quorum_mobile`) verbatim, but its underscore is not a valid RFC 3986 URL-scheme character, so `flutter_web_auth_2` rejected every attempt instantly and silently. Fixed (`com.quorum.quorummobile`, underscore removed) across the mobile app, the Android manifest, and the live backend's own redirect target, redeployed (`quorum-backend-00005-qhq`), and confirmed live: a real person completed Google's real consent screen, landed in the real `MainShell`, and `/trust_digest`'s real live data plus the real account-deletion UI (correctly gated) were both confirmed working against the real signed-in account. **Roadmap Phase 3, the real Today-screen backend, is now closed (`DEC-119`):** a new `negotiations` table plus a `user_id` retrofit on `action_events` (migration `0004`, applied live to the real Supabase database), `features/today.py`, and a real, live `GET /today` — redeployed (`quorum-backend-00006-k29`) and confirmed on the real physical device against the real signed-in account: `8.0h`/`100%` capacity/budget, an honest empty `needs_you_now`/`in_motion` (no producer pipeline exists yet to fill either — the same disclosed "correct but empty" shape as `/trust_digest`), and the Holding Steady → Tasks drill-through link genuinely reachable on screen for the first time. **Roadmap Phase 4a, real Unified Fast Search, is now closed (`DEC-120`)** — the backend's first real embedding pipeline (`core/embeddings.py`, real live Gemini calls), migration `0005` correcting `note_embeddings`' never-real `VECTOR(1024)` placeholder to a live-confirmed **768** plus a real HNSW index, `features/search.py`'s lazily-backfilling per-user search, a real `GET /search?q=...`, and `api/search_api.dart` reaching the already-built Search screen. Its CRITICAL-tier pre-merge review returned **FAIL on the first pass with 3 blocking findings, all fixed before merge** — and that review ran *before* deployment, fixing the sequencing nit `DEC-118` and `DEC-119` both disclosed and neither corrected. The most consequential finding changed the shipped schema: the model's own 3072-dimension default would have permanently foreclosed ANN indexing (pgvector refuses above 2000, confirmed live), silently condemning every search to a sequential scan — caught while `note_embeddings` still held zero rows, the cheapest moment it would ever be. A second finding revealed **this project's CI had been red on `main` since the `DEC-119` merge**: `ci.yml` applied a hardcoded migration list that went stale the moment `0004` landed, and that merge was reported verified on local runs alone without checking CI afterward. Fixed at the root — the step now globs all migrations in sorted order. **The negotiation-detail gap and the backend's first real Stage-B-style LLM content-generation call are now both closed (`DEC-121`)** — `negotiation/gemini_calls.py`'s real Gemini-backed `PositionCall`/`SynthesisCall` factories (two real bugs found and fixed via live testing: the model's own returned option IDs and option counts are no longer trusted, both assigned deterministically in code), migration `0006` adding real `positions`/`options` persistence to `negotiations`, and a real, live `GET /negotiations/{negotiation_id}` (`QUORUM_DATA_CONTRACTS.md` §5.5a, newly written — no contract for this existed anywhere despite `NegotiationBundle` needing it since `MOBILE_09`) — genuinely unlocking Today's In Motion cards as real, tappable drill-throughs for the first time. Blocked short of full live re-verification by a real, live, external quota exhaustion (open item #21) — disclosed rather than routed around. **The real demo dataset is now real and live (`DEC-122`)** — `scripts/seed_demo_dataset.py`, seeded under the one real (non-test) account this database has, confirmed on the real physical device: Today's real "Send an email" needs-approval card, `6.0h`/`94%` capacity/budget correctly computed from real seeded tasks/expenses, "Finance vs. Tasks — Awaiting your choice" (the real negotiation card), all 7 real tasks correctly sorted, all 5 real applications correctly grouped by status, a real, live-detected Spotify subscription, and a real semantic Search query genuinely ranking real cross-domain content — the first time this project's entire real, live backend and mobile pipeline has been demonstrated end to end against real, believable content rather than empty states. A real, disclosed mistake happened and was fixed the same session: an early version of the seed script needed `--force` (a full re-seed) just to retry the deferred negotiation-detail step, which genuinely duplicated every seeded row — caught live, cleaned up, and fixed at the design level. **`POST /negotiations/{negotiation_id}/choose` is now real and live (`DEC-123`)** — a person can finally act on a negotiation, not just see one, closing the gap `DEC-104`/`DEC-121` both disclosed. `retry_queue` (real since this backend's earliest schema, confirmed unused by any real code path before this session) turned out to be the exact, already-existing mechanism §5.6's own "enqueued" language was describing — real per-user scoping, real transactional atomicity, real typed errors mapped to real distinct HTTP statuses. Found and disclosed a real, separate correctness gap along the way: `DELETE /account` never purges `action_events`/`negotiations` (a stale code comment previously claimed otherwise), tracked as its own CRITICAL-tier open item rather than patched incidentally. **That gap is now closed (`DEC-124`)** — `purge_postgres_rows()` deletes a user's real `action_events`/`negotiations` inside the same real transaction as every other per-user table, verified at both the store layer and the real `DELETE /account` HTTP layer, cross-user isolation proven for both tables specifically. `retry_queue` restated as genuinely, permanently out of scope — no per-user ownership column exists on it, a real design limitation rather than a gap. **The Gate's own Stage B — Critic and Judge — is now real for the first time in this project's history (`DEC-125`)** — `orchestration.py`'s `CriticCall`/`JudgeCall` had existed as pure type signatures since `IMPL_08`, exercised only against hand-written fakes, confirmed by direct search before this session began. `gate/prompts.py` gains real `CRITIC_SYSTEM_PROMPT`/`JUDGE_SYSTEM_PROMPT` (closing a real, disclosed mismatch: `QUORUM_GATE_SPECIFICATION.md` had claimed these already existed with "4/4 passing" tests — they did not, confirmed directly against both the module's own honest docstring and the real, then-2-test `test_gate_prompts.py`). `gate/anonymization.py` (new) provides a real, seedable `randomize_objection_order()` — the code-level half of Sec 5.3's "role-stripped, order-randomized" Judge design (the other half, role-stripping, turned out to need no code at all: `Objection` has no identity field to strip in the first place). `gate/llm_calls.py` (new) provides the real `make_groq_critic_call()`/`make_gemini_judge_call()` factories. **A real, disclosed deviation from the spec's named Critic model, found live before hardcoding anything:** `QUORUM_GATE_SPECIFICATION.md` Sec 5.1 names "Groq-hosted Llama 3.3 70B"; a real, live call to that model ID returned a 404, and a real, live `GET /openai/v1/models` against this project's own Groq key confirmed the current catalog has no Llama chat model left at any size. `openai/gpt-oss-120b` — the largest general-purpose model in that real catalog — is used instead, preserving the actual load-bearing property (a genuinely different provider and model family from Gemini) even though the specific historical name is gone. Confirmed live and working, including real strict `json_schema` structured output. These new Stage B implementations are NOT yet wired into any real production call path (`self_test_harness.py`'s adversarial scenarios deliberately keep their own deterministic fakes, since they test orchestration logic, not model judgment) — that wiring is real, disclosed, separate follow-on work for the `retry_queue` drainer (item #26). **A real, disclosed process deviation, not silently absorbed:** the automated CRITICAL-tier review subagent stalled twice on this branch (once for ~1hr, once for ~45min after a tighter retry) and never produced findings; a direct, adversarial self-review was substituted instead, using the same checklist — a genuine reduction in review independence, not equivalent, flagged honestly rather than presented as the standard process having completed. That self-review found and fixed one real bug before merge: `make_groq_critic_call`/`make_gemini_judge_call` didn't catch `pydantic.ValidationError` on a malformed enum value from the model, raising a raw, inconsistently-typed exception instead of the module's own documented `GateLlmCallError` (not a fabricated-verdict risk either way — orchestration.py's own outer retry catches `Exception` broadly — but a real contract inconsistency, fixed with two new tests). **A real cleanup pass (`DEC-126`)** corrected `DEC-125`'s own overgeneralized "quota resolved" claim (the Gemini quota genuinely fluctuates — different real calls minutes apart can land on opposite sides of it), deleted the four orphaned `test-`-prefixed `users` rows via the real, already-tested `delete_account()` path, and found `fetchGateReveal` (item #24) to be genuinely bigger than client wiring — no `findings`/`objections` persistence or backend route exists for it at all, disclosed rather than faked. **The `retry_queue` drainer itself is now real (`DEC-127`)** — `STATUS_INDEX.md` item #26, the largest remaining open item, closed. `negotiation/downstream_translation.py` (new) provides real, live Gemini-backed translation from a chosen negotiation option's free text into structured arguments for whichever of the 3 real negotiation-eligible domains (`finance`/`tasks`/`calendar` — `Position.domain`'s own schema constraint) it belongs to; a real option's `source_domains` can name more than one, so one chosen option can genuinely produce multiple real downstream actions, each independently re-entering the Gate (`QUORUM_DATA_CONTRACTS.md` §5.6's own "each" — read literally, not as a hypothetical). `features/retry_queue_drainer.py` (new) is the real orchestrator: real `SELECT ... FOR UPDATE SKIP LOCKED` dequeue, real Stage A (`provenance_check` always, `deadline_conflict_check` for `tasks` specifically, backed by a real, live query against the real `tasks` table — `budget_check`/`availability_check` stay out of scope, disclosed, since neither a real budget-ceiling concept nor a `calendar_events` table exists anywhere in this schema), the real `DEC-125` Critic/Judge for Stage B, and a real, exhaustive mapping from the resulting `GateVerdict` onto `action_events`'s own real `gate_decision`/`outcome`/`resolved_at` columns — an `escalate_to_human` verdict now genuinely, correctly leaves a real, still-open `action_events` row, closing a small, real piece of `/today`'s own disclosed "correct but empty `needs_you_now`" gap (`DEC-119`) the honest way, by finally being a real producer for a table that endpoint already reads, not by fabricating content. Exposed via a new, real `POST /internal/drain-retry-queue`, gated by a real, static, timing-safe-compared shared secret (`X-Internal-Secret`/`INTERNAL_DRAIN_SECRET`) rather than a user Bearer token, since `pg_net` has no real user session. **Two real, disclosed scope boundaries, decided deliberately, not discovered as blockers partway through:** this drainer produces a real Gate *verdict* per downstream action and stops — it does not execute the approved action's real-world effect (confirmed by direct search: no code anywhere in this backend has ever executed a Gate-approved proposal, for any action, ever); and `pg_cron`/`pg_net`, confirmed live NOT enabled on the real Supabase project despite `IMPL_10`'s own setup instructions naming that step, remain a real, manual, dashboard-only action for Preethish — `backend/scripts/enable_retry_queue_drain_cron.sql` has the real, ready-to-run SQL, genuinely unverified live until then. **The real execution layer is now real, for the two domains genuinely safe to close it for (`DEC-128`)** — `features/action_executor.py` (new): a real, approved `CREATE_TASK`/`LOG_EXPENSE` verdict now genuinely writes a real `tasks`/`expenses` row, on the same transaction as the decision that authorized it, never called for `reject`/`revise`/`escalate_to_human`. Every other real `ActionType` still returns an honest, non-executing result, each with its own real, disclosed reason (no `budgets`/`calendar_events` table, or a genuine external API call this project treats as separate, Rule-5-gated scope). A real, disclosed schema gap found and closed the same session: `expenses.source`'s real, closed 3-value `CHECK` had no value for a Gate-executed row — migration `0007` (new) adds `gate_approved`, applied live. A second real, disclosed gap: `expenses` has no `category` column at all — a real, translated category survives in `action_events.payload` but isn't persisted onto the `expenses` row itself.

| Layer | Status |
|---|---|
| `backend/src/quorum_backend/gate/schemas.py` | Real, tested. Full `QUORUM_DATA_CONTRACTS.md` §1 schema set (`ActionType`, `EvidenceRef`, `Finding`, `Objection`, `ContextSnapshot`, `ActionProposal`, `GateVerdict`, `Stakes`, `ResourceClaim`, `Position`, `NegotiationOption`, `ImpactDelta`) — all real, all in active use. |
| `backend/src/quorum_backend/gate/validators.py` | **All 9 real Stage A validators — Stage A is complete.** `budget_check`, `temporal_fact_check`, `availability_check` (`IMPL_01`), `deadline_conflict_check` (`IMPL_02`), `recipient_check` (`IMPL_03`), `commitment_check` (`IMPL_04`), `pii_leak_check` (`IMPL_05`) — full integration with the Privacy Gate's real flagged-span output (`mobile/lib/privacy/`, real as of Batch 5) is still a separate, open cross-platform wiring item, not yet done — `provenance_check` (`IMPL_06`, CRITICAL, manually reviewed, DEC-056), `coverage_check` (`IMPL_07`, one honestly-documented limitation, DEC-057). |
| `backend/src/quorum_backend/gate/orchestration.py` | **Real, tested — CRITICAL tier.** `review()`, `run_stage_a()`, `stage_a_hard_fail()`, `run_stage_b()`, `_call_with_retry()`, `InfrastructureFailure`. Manually reviewed (DEC-058). **`CriticCall`/`JudgeCall`'s first real implementations now exist (`DEC-125`)**, in `gate/llm_calls.py` — `make_groq_critic_call()` (Groq, `openai/gpt-oss-120b`, a real, disclosed substitute for the spec's since-removed "Llama 3.3 70B") and `make_gemini_judge_call()` (Gemini, `gemini-3.6-flash`, anonymizing objection order via `gate/anonymization.py` before prompting). `gate/prompts.py` gains real `CRITIC_SYSTEM_PROMPT`/`JUDGE_SYSTEM_PROMPT`, correcting a real spec/code mismatch `QUORUM_GATE_SPECIFICATION.md` had carried. **Real production wiring now exists (`DEC-127`):** `features/retry_queue_drainer.py` calls the real `review()` with these real factories for every real, dequeued `retry_queue` job — see open item #26 (RESOLVED) below. |
| `backend/src/quorum_backend/router.py` | Real, tested (`IMPL_09`). `STAKES_TABLE` (all 11 real `ActionType`s), `get_stakes()`, `Complexity`/`ComplexitySignals`/`compute_complexity()`. |
| `backend/src/quorum_backend/auth/` | Real, tested (`IMPL_12`). `access_token.py` (STANDARD), `refresh_token.py`/`oauth_pkce.py` (CRITICAL, manually reviewed, DEC-062). **Wired to real, live routes and a real database as of Batch 10 Phase 3 (`DEC-101`):** `refresh_token.py` converted to async, with a real, atomic `claim_and_rotate()` closing a genuine concurrent-reuse race (`--max-instances=2` makes it real, not hypothetical) — this method exists in its current, correct, transactional form specifically because this session's own mandatory pre-merge review caught a real, confirmed-exploitable gap in its first version (an earlier `try_claim()` correctly made the old token's claim atomic but left the race winner's new child token's insertion unordered against the loser's revocation — empirically reproduced by the review, fixed before merge, full account in `DEC-101`). Proven correct both in-memory (a forced `asyncio.Barrier` race) and against the real, live database, including a direct postcondition check that the race winner's own new token ends up genuinely revoked. `revocation_store.py` (new) is the real Supabase-backed store `IMPL_12` deferred. `google_oauth.py` (new) is real, live Google OAuth code exchange + `id_token` verification. `main.py` now exposes real `POST /auth/token`/`/auth/refresh`/`/auth/revoke` and a real Bearer-auth gate on `GET /trust_digest`. **`user_provisioning.py` (new, `DEC-110`)** — a real, separate identity-mapping layer (a new `users` table bridging Google's `sub` onto the internal UUIDs domain tables use), deliberately not touching this module's own CRITICAL-tier session-management internals at all; `/auth/token` calls it once per real sign-in. |
| `backend/src/quorum_backend/agents/` | **All 5 real domain agents complete** (`IMPL_13`–`17`): `tool_authorization.py`, `email_agent.py`, `calendar_agent.py`, `tasks_agent.py`, `finance_agent.py`, `career_agent.py`. Full 5-domain exhaustive authorization matrix — 60 real cross-domain checks, zero violations — proven live. |
| `backend/src/quorum_backend/negotiation/` | **Complete, real, end-to-end** (`IMPL_18`–`21`): `trigger.py` (pure computation, zero LLM calls, 2+ conflicted-domain threshold), `positions.py` (real `asyncio.gather` concurrency), `synthesis.py` (merge-not-invent mechanically enforced via `validate_synthesis_shape`), `impact_simulator.py` (pure arithmetic, `task_hours_committed`'s inverted polarity built correct from the start — see DEC-070), `subgraph.py` (the real, compiled LangGraph pipeline wiring all four together, proven to compose on the first real end-to-end attempt — DEC-071). **Negotiation — the project's headline capability — is real.** |
| `backend/src/quorum_backend/security/` | **Complete, real** (`IMPL_22`): `trace_scrubbing.py` (`SENSITIVE_PATTERNS` matching `QUORUM_CONFIGURATION_CONSTANTS.md` §10.1 exactly; typed, diagnosable redaction placeholders, never silent deletion), `account_deletion.py` (`delete_account()` reuses `auth.refresh_token.revoke_all_for_user()` rather than reimplementing session revocation — see DEC-072; converted to `async` alongside `refresh_token.py`'s own async change, `DEC-101`). |
| `backend/features/*` | Mostly not yet built — `style_reply.py`, `waiting_on.py`, `meeting_load.py`, `predictive_risk.py`, `subscription_detective.py`, `career_digest.py`, `honesty_log.py`, `search.py` from the ADD §9.7 table remain absent. **`self_test_harness.py` is now real as of Batch 10 Phase 3 Part A (`DEC-099`)** — wired directly to the real `gate.review()`, no stub layer ever built (none was needed; the real Gate has been live since `IMPL_08`). **`trust_digest.py` is now fully real end-to-end as of Batch 10 Phase 3 Part B (`DEC-100`)** — `aggregate_weekly_summary()` and `fetch_trust_digest()` query the real, live `action_events` table directly (the backend's first real database query, ever — `core/db.py` is a new, real `asyncpg` connection layer built to make this possible), wired to a real, live `GET /trust_digest` (`main.py`). **A further real exception, genuinely built during mobile work, not part of the ADD table's original list:** `security/memory_transparency.py` (`MOBILE_19`, `DEC-091`). **`today.py` is now real as of Batch 10 Phase 3 (`DEC-119`)** — `compute_capacity_state()`/`compute_budget_state()` (pure, hand-verified-parity ports of the mobile `computed_state.dart` logic) plus four real, per-user-scoped live queries (`fetch_pending_actions`, `fetch_active_negotiations`, `fetch_today_capacity`, `fetch_today_budget`), wired to a real, live `GET /today`. `computed_state.py` (the Python reference for the *rest* of the app's computed-state logic) still remains open — only the Dart port (`mobile/lib/features/computed_state.dart`, Batch 6) and this session's own narrower `today.py` port are real so far. **`search.py` is now real as of Roadmap Phase 4a (`DEC-120`)** — a real, per-user-scoped pgvector similarity query over a real, live Gemini embedding pipeline (`core/embeddings.py`, new — the backend's first real production call to an external LLM-family API), with a lazily-backfilling, hard-capped embedding step and a real HNSW index. `waiting_on.py` and a real `mem0` integration remain genuinely absent, now with real scope decisions attached (open items #18–20). **A real, disclosed correction, found during a later staleness pass:** this cell's own list above is itself stale in several places predating `DEC-149` -- `waiting_on.py` (`DEC-140`), `career_digest.py` (`DEC-147`), `honesty_log.py` (`DEC-145`), and now `predictive_risk.py` (`DEC-149`) are all real; only `style_reply.py`/`meeting_load.py`'s own read-side wiring beyond `today.py`'s narrow reuse and a real `mem0` integration remain genuinely absent from the ADD §9.7 list. Not rewritten in full here — see the Product Reality section at the top of this file and the relevant `DEC-1xx` entries for the current, accurate state; this row is left as a historical artifact rather than silently edited into a false impression of having always been current. |
| Mobile (`mobile/lib/**`) | **ALL 23 MOBILE SESSIONS COMPLETE — Batch 5 (`MOBILE_01`–`04`), Batch 6 (`MOBILE_05`–`09`), Batch 7 (`MOBILE_10`–`14`), Batch 8 (`MOBILE_15`–`19`), and Batch 9 (`MOBILE_20`–`23`, closing the mobile sequence).** Batch 9: `features/outage/outage_detector.dart` + `action_disposition.dart` (CRITICAL tier — the literal enforcement of the absolute S3-during-outage rule, exhaustively proven across all 8 stakes×outage combinations, `DEC-093`) + `outage_banner.dart`; `features/share_intent_logic.dart`, `share_intent_handler.dart`, `today_widget_bridge.dart`, `pending_share_provider.dart` (two files this batch's own narrative described as pre-existing "dormant code" that never actually existed in this repository — built fresh, `DEC-094`); `features/today_screen.dart` + full real composition of `main_shell.dart`'s four tabs (a genuinely new session, two disclosed design differences from the assumed Scaffold-wrapping and nested-scrollable-crash narrative — zero real screens here wrap `Scaffold`, zero Today zones are internally scrollable, `DEC-095`); `features/tasks/tasks_logic.dart`+`_screen.dart` (the last of 46 original sessions — `tasks.status`'s real, closed `CHECK` constraint correctly drives a fail-loud parse, the deliberate opposite of Career Pipeline's open-vocabulary fallback, `DEC-096`). Batch 5: `pubspec.yaml`, `main.dart`, `shell/main_shell.dart` (4 fixed tabs), `theme/quorum_theme.dart`, `db/database.dart` (4 Drift tables + `.forTesting()`), device tiering (`config/model_config.dart`, `model/device_tier.dart`, `model/on_device_model_loader.dart` — `resolvedFullTierModel` honestly still `OnDeviceModelId.unresolved`), Privacy Gate (`privacy/rule_layer.dart`, `privacy/privacy_gate.dart` — rule-layer parity with `backend/security/trace_scrubbing.py`, confirmed character-for-character), `features/calendar_sync.dart`. Batch 6: the Today screen's three zones — `features/today/needs_you_now_logic.dart`+`_zone.dart`, `features/today/holding_steady_logic.dart`+`_zone.dart`, `features/today/in_motion_logic.dart`+`_zone.dart` — plus a real, newly-built `features/computed_state.dart` (Dart-side prerequisite `MOBILE_06` needed but this repository never had, see `DEC-078`); the Gate Reveal (`features/gate_reveal/gate_reveal_logic.dart`+`_screen.dart`, `theme/quorum_theme.dart` extended with `QuorumStatusColors` — all four colors built fresh, see `DEC-080`); the full Negotiation Screen (`features/negotiation/negotiation_logic.dart`+`_screen.dart` — confirmed live that the backend's `task_hours_committed` polarity fix, `DEC-070`, is genuinely in place before trusting this screen's displayed directions). A new shared helper, `lib/gate/action_types.dart`. Batch 7: `features/waiting_on/waiting_on_logic.dart`+`_screen.dart`, `features/career/career_pipeline_logic.dart`+`_screen.dart` (a real, confirmed open `applications.status` vocabulary, `DEC-083`), `features/career_digest/career_digest_logic.dart`+`_screen.dart` (the not-yet-compiled vs. loaded-empty three-state distinction, `DEC-084`), `features/finance/finance_logic.dart`+`_screen.dart` (the Dart `.5`-rounding open item confirmed on a second real file, `DEC-085`), `features/search/search_logic.dart`+`_screen.dart` (the first genuinely clean contract check in the mobile sequence, `DEC-086`) — every session in this batch found `backend/features/*.py` absent and built against `DATA_CONTRACTS.md`'s real JSON contracts directly. Batch 8: `features/honesty_log/honesty_log_logic.dart`+`_screen.dart` (the caught-by-gate vs. corrected-by-user distinction preserved, a single-scroll design chosen over a TabBar, `DEC-087`); `features/trust/trust_logic.dart`+`_screen.dart` (a fabrication declined — `backend/features/self_test_harness.py` does not exist here, so no backend "staleness fix" was invented against it, `DEC-088`); `backend/src/quorum_backend/features/trust_digest.py` (new, real backend module) + `features/trust_digest/trust_digest_logic.dart`+`_screen.dart` (genuinely new work, correctly distinguished from `MOBILE_16`'s deferred finding, `DEC-089`); `features/you/you_logic.dart`+`_screen.dart` (built against this repository's real, already-shipped `DeletionResult` — a boolean `sessions_revoked`, not the batch's assumed device count, `DEC-090`); `backend/src/quorum_backend/security/memory_transparency.py` (new) + `features/memory_transparency/memory_transparency_logic.dart`+`_screen.dart` (mirrors the real backend's `group_by_category()` exactly, `DEC-091`) — two genuinely new real backend modules built during this batch alone. **VERIFIED FOR REAL as of Batch 10 (`DEC-103`)** — `flutter analyze` and `flutter test` genuinely ran against the whole tree for the first time in this project's history: zero analyzer issues (after real fixes: generating the missing `database.g.dart` via `build_runner`, five real `const` corrections, one real unused import, `sqlite3` promoted to a direct dependency), 225/225 real tests passing (after fixing one real test-authoring bug in `main_shell_composition_test.dart` — a `ListView` viewport/scroll gap, not a production defect, found and fixed the same session). The Dart `.5`-rounding uncertainty (open item #11) and the two flagged package-API uncertainties (open item #13) are both resolved — see below. **A real, new mobile module also now exists:** `api/trust_digest_api.dart` — the first real HTTP-calling code in this codebase's history, wired to the live `/trust_digest` backend, live-verified end to end with a real minted token against the real public URL (`DEC-103`). |
| `backend/migrations/0001_initial_schema/up.sql` + `down.sql` | Real, proven against a real, local Postgres 16 + pgvector. |
| `backend/migrations/0004_today_persistence/up.sql` + `down.sql` | **New, `DEC-119`.** Adds `action_events.user_id` (+ index) and a genuinely new `negotiations` table (+ index) — applied live to the real, production Supabase database, confirmed via `information_schema.columns`. (`0002`/`0003` were absorbed into other sessions' own work — see `DEC-110`'s `users` table and `DEC-113`'s deletion-store schema needs; no separate migration directories were ever created for those.) |
| Cloud Run / Docker | Real `Dockerfile` + `.dockerignore` + `.gcloudignore` (`DEC-160`) + compose/service templates (`IMPL_11`). **A real Cloud Run service is live** (`quorum-backend`, `asia-south1`) — built via Cloud Build (no local Docker dependency). **A real, disclosed correction, caught by `DEC-161`'s own CRITICAL-tier review while it was already open for a different fix:** this row previously hardcoded revision `quorum-backend-00006-k29` "as of `DEC-119`" -- a real number, real at the time, left unstated-as-current ever since, that had gone stale across dozens of real deploys without anyone noticing, the exact drift pattern `CLAUDE.md` itself warns against. Deliberately not replaced with a fresh hardcoded number here, to avoid repeating the identical mistake -- the real, current revision is whatever `gcloud run revisions list --service=quorum-backend --region=asia-south1` says right now, and the deployment-record `DEC-1xx` entries in `DECISIONS_LOG.md` (most recently `DEC-159`/`160`/`161`) are the real, dated history of every one that shipped. **`--no-allow-unauthenticated` relaxed to `--allow-unauthenticated` as of `DEC-102`** — the real, reviewed, race-safe login system (`DEC-101`) is now the actual security boundary, not Cloud Run's IAM layer. **`DEC-114`: the deployment was rebuilt from current `main` and redeployed**, closing a real gap where the live service had been nine sessions stale (`DEC-106`–`113`'s real routes returned live `404`s until this fix, confirmed by direct `curl`) — and `JWT_SIGNING_KEY` was rotated off the known, public default (`"change-me-in-real-deployment"`), proven live by confirming a token forged with the old default is now genuinely rejected. **`DEC-119`: redeployed again to ship `GET /today`**, confirmed live: unauthenticated `GET /health` → real `200`; unauthenticated `GET /today`/`/trust`/`/tasks`/`/career_pipeline`/`/finance/subscriptions`/`DELETE /account` → real `401` (the app-level gate correctly demanding auth on every real route, not a `404` of a route that doesn't exist); `GET /trust_digest` with a real, valid access token → real `200` with real database data. |
| CI pipeline | Two real, live, working GitHub Actions workflows exist. `.github/workflows/test-gcp-auth.yml` (`DEC-098`) proves Workload Identity Federation auth end-to-end. **`.github/workflows/ci.yml` (new, `DEC-116`) is real, basic, PR-gating CI** — `ruff`/`pytest` (against a real, disposable Postgres 16 + pgvector service container, never the live Supabase project) plus `flutter analyze`/`flutter test`, on every real PR and push to `main`. Proven live, not just written: PR #1 caught two real, previously-unknown gaps on its own first run (two tests needing a live Google OAuth secret this environment can't provision as a repo secret — both explicitly excluded, disclosed directly in the workflow file itself, tracked as open item #17's own scope), then passed clean (267/268 backend + full mobile suite) before merge. **A real, disclosed failure of that gate, found by `DEC-120`'s review and fixed there (`DEC-120`):** the migration step applied a hardcoded list (`0001`/`0002`/`0003`) and went stale the instant `DEC-119` added `0004`, so **CI was genuinely red on `main` from the `DEC-119` merge until `DEC-120`** — confirmed directly via `gh run list`. `DEC-119` was reported verified on the strength of four local commands passing, without anyone checking CI after the merge; the gate existed and was simply not looked at. Fixed at the root rather than by appending two lines: the step now globs `migrations/*/up.sql` in sorted order with `ON_ERROR_STOP=1`, so it cannot go stale again when `0006` lands. **A real build→test→deploy pipeline now exists (`DEC-160`, Phase 9-A):** `ci.yml` gained a real `deploy` job, gated on both real CI jobs passing and a genuine push to `main`, automating the exact `gcloud builds submit`/`gcloud run deploy` sequence every manual deploy since `DEC-098` has run by hand. The real work was almost entirely IAM discovery -- `quorum-ci-deployer` held only one role before this session, genuinely insufficient for a full build+deploy; six real permission gaps were found and fixed by live-testing the actual operations via impersonation before trusting them in a real workflow run, each scoped as narrowly as a working fix allowed (one new custom role created rather than granting broad `storage.admin`; the `--allow-unauthenticated` flag deliberately omitted from the automated job rather than granting `quorum-ci-deployer` `setIamPolicy` rights it doesn't otherwise need). Full real verification (a full build + a full `--no-traffic` deploy, both via impersonation) passed before merge. **The first real trigger, watched closely as promised, found a real, live production bug within minutes (`DEC-161`):** the job's own first automated run built and deployed a genuinely new, healthy revision, but the live service kept routing 100% of real traffic to the OLD revision -- a real, permanent, documented side effect of this same session's own earlier `--no-traffic` test deploys (`gcloud`'s own docs: once used, "the LATEST revision will not receive traffic on future deployments" until explicitly restored). Fixed immediately, live (`gcloud run services update-traffic --to-latest`), and fixed structurally in `ci.yml` -- the deploy step now reasserts `--to-latest` on every run, and the verify step now explicitly confirms the latest-ready revision matches the one actually serving traffic before it ever checks `/health` (a check any healthy revision would have passed identically, which is exactly why it missed this the first time). **The pipeline is now real, live, and has already survived one genuine incident on its own first run, not merely a passing first attempt.** |
| **Backend total, this repository** | **412/417 real, passing tests** (`ruff check backend` clean), a real, live, full-suite run — the 5 real, live, unmocked failures are all the same, already-disclosed, fluctuating `gemini-3.6-flash` quota `429`s (open item #21) split across `test_negotiation_gemini_calls.py`/`test_downstream_translation.py`, confirmed by directly re-checking every failure's own error text before treating them as external, not a new regression from this session's own execution-layer work (which has zero Gemini dependency of its own). `PYTHONPATH=backend/src pytest backend/tests -q`, run from `backend/` so `.env` resolves. `DEC-125` added `gate/prompts.py`'s Critic/Judge prompt tests (`test_gate_prompts.py` +7), `gate/anonymization.py`'s tests (`test_gate_anonymization.py`, new, +5), and `gate/llm_calls.py`'s tests (`test_gate_llm_calls.py`, new, +10). `DEC-126` added 0 net (doc-only). `DEC-127` added `test_downstream_translation.py` (new, +9), `test_retry_queue_drainer.py` (new, +18), and 4 new tests in `test_main.py` for `POST /internal/drain-retry-queue` — net +31 (377 + 31 = 408). `DEC-128` added `test_action_executor.py` (new, +6), extended several existing `test_retry_queue_drainer.py`/`test_main.py` tests with real execution assertions (net +0 new functions), then added 3 more real regression tests during its own CRITICAL-tier review (2 real numeric-upper-bound tests, 1 real live transaction-boundary test proving the review's own more serious finding is genuinely fixed) — net +9 (408 + 9 = 417), reconciling exactly against the real, live count. The original 23-session core permanently closed at 151 (`DEC-069` through `DEC-072`); Batch 8 added two new real modules during mobile work — `features/trust_digest.py` (`MOBILE_17`, +7) and `security/memory_transparency.py` (`MOBILE_19`, +4); Batch 10 Phase 0 (`DEC-097`) added `core/config.py` (+10); Batch 10's credential-verification work extended `core/config.py` again (+2, `DEC-099` retroactively documents this real, already-merged gap); Batch 10 Phase 3 Part A (`DEC-099`) added `features/self_test_harness.py` (+7); Batch 10 Phase 3 Part B (`DEC-100`) added real, live database tests for `core/db.py`/`trust_digest.py`'s new aggregation query and the real `/trust_digest` endpoint (+6); Batch 10 Phase 3 Part C prerequisite (`DEC-101`) added the real auth-routes/Google-OAuth/race-safety work, including a real vulnerability found by pre-merge review and fixed before it ever reached `main` (+22); Track B (`DEC-105`) added the real `/auth/callback` bridge route and further auth-route hardening (+7), then removed 3 genuine near-duplicate `/auth/callback` tests (a pre-restart-session leftover, flagged LOW-severity/non-blocking by the merge review and cleaned up in the same branch before merge); Track C item 1 (`DEC-106`) added the real `GET /trust` endpoint (+3); Track C item 2 (`DEC-107`) added the real `GET /tasks` endpoint (+7); Track C item 3 (`DEC-108`) added the real `GET /career_pipeline` endpoint (+7); Track C item 4 (`DEC-109`) added the real `GET /finance/subscriptions` endpoint (+12); real user provisioning + per-user data isolation for `/tasks`/`/career_pipeline`/`/finance/subscriptions` (`DEC-110`) added the real `users` table, `auth/user_provisioning.py`, and cross-user-isolation proofs (+14); a real correction to `subscription_detective.py`'s detection parameters (`DEC-112`) added +4 net; the real `DELETE /account` endpoint (`DEC-113`) added `security/supabase_deletion_store.py` and its own extra-rigorous, S3-equivalent test suite (+11); Roadmap Phase 3 (`DEC-119`) added `features/today.py` + the real `GET /today` endpoint (`test_today_feature.py` +21, `test_main.py` +4, net +25); Roadmap Phase 4a (`DEC-120`) added the real embedding pipeline and Unified Search (`test_embeddings.py` +9, `test_search_feature.py` +11, `test_main.py` +7, net +27 — the `test_main.py` additions include a real, permanent security regression test asserting the live API key can never reach a `/search` 502 body); the negotiation-detail work (`DEC-121`) added the backend's first real Gemini content-generation calls plus negotiation-detail persistence (`test_negotiation_gemini_calls.py` +8, `test_negotiation_detail_feature.py` +4, `test_main.py` +5, net +17); the negotiation-choice work (`DEC-123`) added the real `POST /negotiations/{id}/choose` endpoint (`test_negotiation_choice_feature.py` +7, `test_main.py` +7, net +14); the account-deletion purge-completeness work (`DEC-124`) added `action_events`/`negotiations` purge coverage (net +2); the Gate's real Stage B work (`DEC-125`) added `test_gate_prompts.py`/`test_gate_anonymization.py`/`test_gate_llm_calls.py` (net +20) — 172 → 181 → 187 → 209 → 216 → 213 → 216 → 223 → 230 → 242 → 256 → 260 → 271 → 296 → 323 → 340 → 354 → 355 → 377. **A real, found-and-closed discrepancy in this row's own prior entry, not a new one:** this line previously said `DEC-123` left the total at 354, but `DEC-123`'s and `DEC-124`'s own DECISIONS_LOG entries each directly report a live `pytest` count of 351+3=354 and then 352+3=355 respectively — `DEC-124` added 0 net test *functions* (it extended 3 existing tests with more real assertions instead) but its own live count (355) was one higher than this row had carried forward. 355 + `DEC-125`'s own, directly-counted +22 (7 + 5 + 10 across its three new/extended test files, the last figure including 2 tests added by `DEC-125`'s own mid-session self-review) = 377, matching the real, live `pytest` total exactly. |
| **Mobile total, this repository** | **334/334 real tests passing, `flutter analyze` clean** (`DEC-103`–`DEC-123`) — 225 (`DEC-103`: real execution of the original 23 sessions' work plus the new `api/` module) + 6 (`DEC-104`: `main_shell_navigation_test.dart`) + 18 (`DEC-105`: 6 `pkce_test.dart` + 9 `auth_controller_test.dart` + 3 real, additive updates elsewhere) + 9 (`DEC-106`: `trust_api_test.dart`) + 11 (`DEC-107`: `tasks_api_test.dart`) + 11 (`DEC-108`: `career_pipeline_api_test.dart`) + 9 (`DEC-109`: `finance_api_test.dart`) + 8 (`DEC-113`: `account_api_test.dart`) + 9 (`DEC-119`: `today_api_test.dart`) + 11 (`DEC-120`: 9 `search_api_test.dart` + 2 `utf8_decoding_proof_test.dart`, the latter a permanent pin-down of package:http's real content-type-to-encoding behavior after a suspected production bug was investigated and genuinely DISPROVEN rather than acted on) + 9 (`DEC-121`: `negotiation_api_test.dart`) + 8 new (`DEC-123`: 7 `negotiation_api_test.dart` choose-fetcher tests + 1 real widget test in `main_shell_navigation_test.dart` proving the full choose flow end to end). Every file across all 23 original mobile sessions plus this batch's new `api/`/navigation/`auth/` work is now genuinely, actually verified against a real compiler — not just structurally correct against documented APIs. `DEC-106` also found and fixed a real, pre-existing, time-of-day-dependent content bug (`holding_steady_logic.dart`'s midday headline literally duplicated its own containing zone's title between 12:00–17:59 local time) — not caused by this batch's own changes, but genuinely broken and unfixed until now; `DEC-113` found and fixed a real, pre-existing missing-error-handling gap in `you_screen.dart`'s own delete-account button. See `DECISIONS_LOG.md` DEC-073 through DEC-096 for the original 23 sessions' history, `DEC-103`–`DEC-119` for the real execution, navigation, login, Trust/Tasks/Career Pipeline/Finance/Account-deletion/Today wiring work. |

## Environment, confirmed real (see `quorum-environment-constraints` for full detail if reading this outside Claude Code)

- Machine: Intel i5-12500H, 16GB RAM, RTX 3050 4GB, Windows.
- Python 3.13.1, `pydantic==2.10.4`, `pytest==8.3.4`, `pytest-asyncio==0.25.0`, `ruff==0.8.4`, `langgraph==1.2.11` — all pinned in `backend/pyproject.toml`, installed via `pip install -e "./backend[dev]"`. **A real, disclosed correction (`DEC-125`):** this line previously claimed a dedicated virtualenv at repo root (`.venv/`); no such directory exists on this real machine, confirmed directly, and this session's own real dependency install (`langgraph` was missing — see the backend test-count row above) went into this machine's one, global Python 3.13 install instead, the same one both PowerShell and the Bash tool resolve to. No functional problem found from this — installs are real and pinned either way — but a genuinely different fact from what this line said.
- Git repository, connected to `https://github.com/Praveen09107/quorum` (public). One branch per session, merged to `main` only after real verification and explicit approval.
- Flutter SDK (`3.47.0`, Dart `3.13.0`, at `D:\dev_tools\flutter`), Android Studio, Android SDK, and a real hardware-accelerated emulator (`medium_phone` profile, Android 14, RAM raised to the real 4096MB the spec requires) are genuinely installed on this machine, as of Batch 10. **`flutter analyze`/`flutter test`/`flutter pub` all confirmed genuinely working as of `DEC-103`** — real, live pub.dev resolution succeeded (`http`, `sqlite3` added as real dependencies); see open item 9 below for what still doesn't (a real device/emulator `flutter run`).
- Cloud accounts: real, live, and free-tier — see open item 7, `DEC-098`. Supabase (`dxfeutkeofnbismljhsb`, `ap-south-1`), Upstash Redis, Langfuse Cloud, Gemini/Groq/Tavily, Google OAuth, and a real Cloud Run service (`quorum-backend`, `asia-south1`) all exist and are individually verified working.

## Known open items — the complete list for this repository

1. **RESOLVED, `DEC-111`, run to genuine completion via a real head-to-head comparison on a real physical device (`DEC-130`/`DEC-131`).** On-device primary model — both real candidates genuinely downloaded, loaded, and ran real inference: Gemma 4 E4B 17% validity, Llama 3.2 3B 67% validity. `decideWinner()`'s real mechanical logic makes Llama 3.2 3B the decided Full-tier primary — a decisive, genuine winner on real, measured accuracy and speed. `mobile/lib/config/model_config.dart`'s `resolvedFullTierModel` updated to match.
2. **RESOLVED, `DEC-111`, reconfirmed on real hardware `DEC-130`.** Flutter llama.cpp plugin selection — `llamadart`, confirmed genuinely working via a real health-check model load on a real physical device.
3. `coverage_check`'s single-shared-stopword limitation — honestly documented (`DECISIONS_LOG` DEC-057), a real, deliberately-accepted trade-off from the original spec, not a bug — noted here only so it isn't lost, not as an action item.
4. `orchestration.py`'s `_call_with_retry` wraps the whole Stage B call, not Critic/Judge independently — a transient Judge-only failure re-invokes the Critic on retry, a real, minor cost inefficiency, disclosed in `DECISIONS_LOG` DEC-058, not required against by anything in the real spec but worth knowing.
5. `MOBILE_03` **is now real in this repository** (Batch 5) — resolved as of `DEC-075`. `pii_leak_check`'s full backend integration with the Privacy Gate's flagged-span output remains a separate, still-open cross-platform wiring item, not yet done.
6. Real demo dataset (simulated-and-real hybrid, all 5 domains) — wanted, tracked, not yet built; needs real backend/schema to load against (Phase 3 per `QUORUM_IMPLEMENTATION_STRATEGY.md`).
7. **RESOLVED, `DEC-098` — real cloud infrastructure now exists and is live.** Supabase (project `dxfeutkeofnbismljhsb`, `ap-south-1`, real migration applied — all 7 tables + `pgvector` confirmed via live query), Upstash Redis, Langfuse Cloud, Gemini/Groq/Tavily API keys, and Google OAuth credentials — all 8 real external credentials individually tested live (not just format-checked). The backend is deployed to a real Cloud Run service (`quorum-backend`, `asia-south1`) with every real, load-bearing flag this project's architecture requires (`--concurrency=1 --min-instances=0 --max-instances=2`), confirmed via a real authenticated `/health` request returning `200`. **`--no-allow-unauthenticated` → `--allow-unauthenticated` as of `DEC-102`** — the real, reviewed, race-safe login system (`DEC-101`) is now the actual security boundary, confirmed via three real live requests (see the Cloud Run row above). GCP CI auth uses Workload Identity Federation (verified live via a real GitHub Actions run), not a downloaded service-account key — see `.github/workflows/test-gcp-auth.yml`. **Cold-start latency, now fully resolved (`DEC-098`, appended once the real wait completed):** rollout-init 2.17s, warm-request baseline 0.124s, and a genuine post-idle cold start (after a real ~18-minute `--min-instances=0` idle wait) of **4.543s**, independently confirmed as a true cold start via Cloud Run's own real logs (`"Starting new instance. Reason: AUTOSCALING..."`).
8. **MOSTLY RESOLVED as of `DEC-149`, a real, disclosed correction to this item's own long-stale claim.** The ADD §9.7 "newly built features" table (Style-Conditioned Replies, Waiting On, Meeting-Load Defense, Predictive Risk, Subscription Detective, Company Research Digest, Self-Test Harness, Honesty Log, Unified Fast Search) -- this item said "none of this is real" for many sessions after most of it actually became real, an unnoticed case of exactly the drift `.claude/CLAUDE.md`'s own drift-pattern #6 warns against. Real as of, respectively: `subscription_detective.py` (early), `self_test_harness.py` (`DEC-099`), `search.py` (`DEC-120`), `waiting_on.py` (`DEC-140`), `honesty_log.py` (`DEC-145`), `meeting_load.py` (`DEC-144`, the real, single-day Python reference; the real, genuine multi-day projection it always named as its own intended destination is now real too, as a real Dart port -- `mobile/lib/features/meeting_load/meeting_load_logic.dart`, DEC-154, computed entirely from real on-device `CalendarMirror` data, zero backend involvement), `career_digest.py` (`DEC-147`), `predictive_risk.py` (`DEC-149`). Genuinely still absent: `style_reply.py`, and a real `mem0` integration for Memory Transparency (blocked on Preethish's own signup). **Computed State is now partially resolved:** `mobile/lib/features/computed_state.dart` is real as of Batch 6 (`DEC-078`) — a genuinely new, disclosed construction since this repository never had the file `MOBILE_06`'s own spec assumed pre-existed. `backend/features/computed_state.py` (the Python reference) remains open.
9. **RESOLVED, `DEC-103`–`105`, `117`–`118`.** The Flutter/Dart SDK gap that blocked every one of Batches 5–9 is closed both at the tooling level (Flutter SDK, Android Studio, Android SDK, a real hardware-accelerated emulator, and — as of `DEC-118` — a real physical Android device) and at the real-execution level: `flutter analyze` (zero issues) and `flutter test` (297/297 passing) genuinely run against the whole mobile tree. **A second, real, previously-undiscovered gap found and closed in `DEC-105`:** `mobile/` had never actually been scaffolded with `flutter create` at all. **`DEC-117`: the real `flutter build apk` + on-device launch happened for the first time**, six real environment/tooling bugs found and fixed along the way. **`DEC-118`: a real, human-completed Google sign-in has now genuinely happened**, on a real physical device (Realme RMX3853), closing the one thing `DEC-117` left open — found and fixed a real, CRITICAL-tier bug in the process (the OAuth callback's custom URL scheme was never a syntactically valid URL scheme, silently breaking every sign-in attempt since the login screen was first built, `DEC-105`) and confirmed the real, live `/trust_digest` endpoint and the real account-deletion UI both work correctly against a real signed-in account. **`DEC-119`: the real Today tab was confirmed rendering live on the same real device**, against the same real signed-in account, after installing a fresh build carrying the new `/today` wiring — a real, second on-device confirmation, not a one-off.
10. **RESOLVED, `DEC-130`/`DEC-131`.** `resolvedFullTierModel` (`mobile/lib/config/model_config.dart`) is now genuinely `OnDeviceModelId.llama32_3B` — `IMPL_00` (Sprint 0) ran to genuine completion on a real Android device, including a real head-to-head comparison: Llama 3.2 3B genuinely downloaded, loaded, and passed real on-device inference (67% validity vs. Gemma's 17%), making it a real, decisive, mechanically-decided winner. `resolveModelForTier(DeviceTier.full)` now returns `'Llama 3.2 3B'` instead of throwing; the real, existing test was updated to match (`mobile/test/model_resolution_test.dart`), `flutter analyze`/`flutter test` reconfirmed clean after the change. Not yet wired into any real user-facing flow — `resolveModelForTier` still has zero real callers anywhere in `mobile/lib/`.
11. **RESOLVED, `DEC-103`.** First tracked in this repository as of `MOBILE_09` (`DEC-081`); confirmed to affect five real files as of Batch 8 (`DEC-092`); now confirmed LIVE against a real Dart 3.13.0 compiler: `(0.505 * 100).round()` → `51`. Dart genuinely uses round-half-away-from-zero, exactly as predicted, genuinely different from Python's banker's rounding (`50`). One real, confirming test added to each of the five affected files (`negotiation_logic.dart`'s `formatMetricValue()`, `finance_logic.dart`'s `formatCurrency()`/`formatInterval()`, `honesty_log_logic.dart`'s `formatSuccessRate()`, `trust_logic.dart`'s `formatCatchRate()`, `trust_digest_logic.dart`'s `formatDelta()`), each exercising the exact tie case its own tests had deliberately left unasserted until now.
12. **RESOLVED, `DEC-104`.** The screen-composition gap, real but genuinely narrower than `MOBILE_22`'s own spec assumed (`DEC-095`), and a real, self-corrected miscount along the way — this line previously said "Nine real, tested screens" while naming exactly seven; direct count confirmed seven was always the real number. All seven (Career pipeline, Company Digest, Finance, Search, Waiting On, the Gate reveal, the negotiation screen) are now genuinely reachable: the Gate reveal and the negotiation screen drill through from Today (Needs You Now / In Motion — the real, disclosed information-architecture decision made and applied); Company Digest drills through from Career Pipeline; Career Pipeline, Finance, Waiting On, and Search are reachable via a new, real "More" section on the You tab. A second, real, pre-existing gap found in the same pass: `TodayScreen`'s own `fetchTasks`/`onTapAction`/`onTapNegotiation` params (including `DEC-096`'s Holding Steady → Tasks link) had never actually been threaded through `main_shell.dart` — closed here too. Two real bugs (a layout overflow, a `setState`/`Future` misuse) caught by this phase's own new tests and fixed. None of the real fetchers are wired to live data yet — see item 14.
13. **RESOLVED, `DEC-103`.** The two real, honestly-flagged package-API uncertainties (`receive_sharing_intent`'s `.instance`/method-name surface in `share_intent_handler.dart`, `home_widget`'s save/update call shape in `today_widget_bridge.dart`) both compiled with zero errors in the same real, clean `flutter analyze` run that resolved item 9 above — both guessed package surfaces were correct.
14. **All 46 original backend and mobile sessions are now complete (`DEC-096`).** Batch 10 — the real, current phase of work — covers **seven** implementation phases (Phase 0 through Phase 6) named in `QUORUM_IMPLEMENTATION_STRATEGY.md`, not six; a real, self-corrected miscount (this line itself said "six" until this entry — the same repeated-miscount pattern the batch's own kickoff separately found and fixed six other instances of, confirmed by direct count of the real document before writing this line). **Phase 0 (Structural Migration) is complete (`DEC-097`)** — confirmed already mostly done on arrival (src-layout, namespaced imports), with `core/config.py` the one genuine gap, now closed. **Phase 2 (Real Infrastructure Provisioning) is complete (`DEC-098`)** — real Supabase, Upstash, Langfuse, Gemini/Groq/Tavily, Google OAuth, a real, live, deployed Cloud Run backend, and a genuine post-idle cold-start measurement (4.543s, confirmed via Cloud Run's own logs) all exist and are verified working; only the multi-day `pg_cron`-vs-Supabase-pause observation (open item #4) remains, and that genuinely needs real elapsed time no single session can compress. **Phase 3 Part A is complete (`DEC-099`)** — `features/self_test_harness.py` is real, wired directly to the live `gate.review()` (no stub layer built, none was needed), with a real test proving a deliberately-mis-specified scenario surfaces as a genuine miss rather than being hidden, per the ADD's own stated design commitment. **Phase 3 Part B is complete (`DEC-100`)** — the backend's first real database query, ever (`core/db.py`, a new real `asyncpg` layer — none existed before), a real weekly-aggregation query in `trust_digest.py`, and a real, live `GET /trust_digest`, redeployed and confirmed live against the actual public Cloud Run URL (see the Cloud Run row above), not just tested locally. **The real prerequisite for Part C is complete (`DEC-101` + `DEC-102`):** Part C's own plan silently assumed the real Cloud Run service could simply be called by a mobile client — it couldn't. Real `POST /auth/token`/`/auth/refresh`/`/auth/revoke` now exist, wiring `IMPL_12`'s already-built, CRITICAL-tier session-management modules into real routes for the first time, backed by a real, race-safe, live-database-backed `RevocationStore` (a genuine, review-caught, review-fixed race in the original design — full account in `DEC-101`) and real Google OAuth code exchange. The Cloud Run network policy has now been relaxed and redeployed (`DEC-102`), confirmed live: an unauthenticated request reaches the app and gets a real, honest `401` from the real login gate, and a request with a real, valid token gets real data back. **Phase 3 Part C-1 is complete (`DEC-103`)** — the first real mobile repository (`api/trust_digest_api.dart`) is wired to the live backend, following this codebase's own established injected-async-function pattern (never an invented `Repository` class), live-verified end to end with a real minted token against the real public URL. **The real mobile login screen is complete (`DEC-105`)** — real Google Sign-In (PKCE, a new `GET /auth/callback` bridge closing a real Google-OAuth-client-type constraint found live), real secure token storage, real proactive refresh, real sign-out; `main.dart` now genuinely checks for a real session at startup and wires the one real, live fetcher that exists (`/trust_digest`) using it. **Still genuinely open:** a genuine, human-completed Google sign-in (an actual person completing Google's real consent screen) — no browser automation exists in this environment; every real piece up to that exact point is built and tested. **Part C-2's first three real items beyond the prerequisite are complete (`DEC-106`–`DEC-108`):** `GET /trust` (wrapping `self_test_harness.py`'s already-real `run_self_test()`) plus `api/trust_api.dart` — this also genuinely unlocks the whole Trust tab in the real running app for the first time, since `DEC-103`/`DEC-105`'s own `/trust_digest` wiring was real and tested but never reachable on screen until the Trust tab's own primary fetcher existed. `GET /tasks` (the real `tasks` table) plus `api/tasks_api.dart` — real and fully tested, but honestly still not visibly reachable on screen: `MainShell` only ever surfaces `fetchTasks` through the Today tab's own drill-through link, itself still gated behind Today's own unbuilt primary fetcher. `GET /career_pipeline` (the real `applications` table, a genuinely open-vocabulary `status`, the deliberate opposite of `tasks.status`) plus `api/career_pipeline_api.dart` — the first Part C-2 item besides Trust to unlock a real, newly-reachable screen outright: the You tab's "More" section shows the Career Pipeline link unconditionally once wired (unlike Today/Trust's own whole-tab gating), confirmed directly against `you_screen.dart` before picking this item. **Track C item 4 (`DEC-109`) closed the Finance gap flagged above:** `GET /finance/subscriptions` -- `detect_subscriptions()` genuinely did not exist anywhere in this backend (confirmed by direct search), the same stale-narrative pattern this file discloses generally, so a real, deliberately simple, disclosed detection rule was designed instead (a payee charged at least twice, exact match only -- no fuzzy matching, no ML), matching this backend's own established "explainable, never a trained model" philosophy. This is also the second Part C-2 item, alongside Career Pipeline, to unlock a real, newly-reachable screen outright via the You tab's unconditional "More" section gating. **A further real, disclosed scope check, done before picking Finance:** Waiting On, Search, and Memories were each checked first and found to need real architecture this repository doesn't have -- Waiting On needs a `sent_messages`-style table absent from the real migration entirely; Search needs real cross-content-type embedding/ranking logic; Memories needs a real `mem0` integration, a genuine external service never wired into this backend. None silently built around; all three remain real, disclosed, larger open items. **A real, blocking correctness gap found and closed (`DEC-110`), CRITICAL tier:** scoping `DELETE /account` surfaced that no user-identity-provisioning system existed anywhere in this backend — every per-user table had a real UUID `user_id` column, but nothing ever mapped a real Google identity onto one. For the three read endpoints above this was a disclosed, honest limitation; for an irreversible `DELETE` it's a genuine safety blocker (a query scoped by an unmapped identity would silently do nothing while looking like success). Stopped and reported rather than built around, per Rule 4/Rule 5. Preethish chose to build real provisioning first: a new `users` table + `auth/user_provisioning.py` (deliberately not touching the CRITICAL-tier session-management system itself — the JWT/refresh-token layer keeps using Google's raw `sub` unchanged), plus a genuine retrofit of `/tasks`/`/career_pipeline`/`/finance/subscriptions` to real per-user filtering, with explicit cross-user-isolation tests proving no leakage. Two real, pre-existing test bugs found and fixed in the same pass (the Finance end-to-end fixtures had been inserting their "recurring charge" test rows under two different random users the whole time — harmless before real filtering, silently wrong once it existed). **`DELETE /account` itself is now real and live (`DEC-113`), CRITICAL tier, reviewed:** a real, blocking identity-conflation bug was found and fixed before any implementation existed to expose it — `delete_account()` had always passed one single `user_id` to both the CRITICAL-tier session-revocation path (needs the real Google `sub`) and the new `DeletionStore` (needs the real internal UUID once `DEC-110`'s `users` table existed); the signature is now two explicit, named, never-conflated parameters. `security/supabase_deletion_store.py` (new) is the first real `DeletionStore` — a real, atomic, single-transaction purge (`interviews` → `applications` → `tasks` → `expenses` → `users`, ordering confirmed against the real FK in `0001_initial_schema/up.sql`, not assumed) plus a separate real `note_embeddings` purge; `purge_memories`/`revoke_oauth_tokens` stay honest, disclosed zeros (no `mem0` integration exists; Google's own tokens are deliberately never persisted here). CRITICAL-tier fresh-context review: PASS WITH NITS, no blocking findings — one doc-comment inaccuracy in `main.dart` fixed as an immediate fast-follow; two nits logged as real, tracked open items below (#15, #16) rather than forcing architecture beyond this session's real scope. **A real, genuine mistake found and fixed in already-merged code (`DEC-112`):** `DEC-109`'s own work checked only `DATA_CONTRACTS.md` §5.12 (response shape) before declaring no detection algorithm was specified, missing that `QUORUM_CONFIGURATION_CONSTANTS.md` §4 separately specifies real parameters for this exact module (min 3 occurrences, ±5.0-day tolerance around a 30-day target) — corrected, with the shipped, more permissive 2-occurrence/any-spacing rule replaced by the real, specified one. **RESOLVED, `DEC-119`:** the Today-screen persistence gap flagged here (`TodayScreenData` needing `pendingActions`/`negotiations`, with no table for either) is closed — migration `0004` added `action_events.user_id` and a new `negotiations` table, both applied live to the real Supabase database, and `GET /today` is real, live, redeployed, and confirmed rendering correctly on a real physical device against a real signed-in account. A real, disclosed, honest scope boundary remains, not a gap: both arrays will genuinely, correctly render empty in production right now, since nothing in this backend yet invokes the Gate against a real, live user action to ever produce a row for either table in the first place — the same disclosed "correct but empty" shape already established for `/trust_digest` (`DEC-100`). Every other still-unbuilt domain (Search, Waiting On, Memories) still needs its own live REST endpoint built first (Part C-2), and each needs real architecture this repository doesn't have yet — see `DEC-109`'s own disclosed scope check. **Phase 5 is substantially complete (`DEC-103`)** — `flutter analyze` and `flutter test` have genuinely run for the first time in this project's history: zero analyzer issues, 289/289 real tests passing as of `DEC-109`, real test-authoring bugs (in `DEC-105`, real production bugs — a layout overflow, a `setState`/`Future` misuse; in `DEC-106`, a real, pre-existing, time-of-day-dependent content collision, unrelated to that session's own new code but genuinely broken until fixed there; in `DEC-109`, a real hand-written test fixture bug -- an out-of-range day-of-month -- caught by the real test run itself and fixed before merge) found and fixed, none shipped. Open items #11 and #13 (the Dart `.5`-rounding uncertainty and two package-API uncertainties) are both resolved as a direct result. **What Phase 5 doesn't yet cover:** a real `flutter run` against a real device/emulator (open item #9). **Phase 4 (Mobile Navigation Completion) is complete (`DEC-104`)** — all seven previously-orphaned screens (a real, self-corrected miscount: this line's own item #12 previously said "nine," direct count confirmed seven) are now genuinely reachable through real, considered navigation (contextual drill-through from Today for the Gate reveal/negotiation screen, Career Pipeline for Company Digest, a new real "More" section on You for the rest) — not an arbitrary set of links. Two real bugs (a layout overflow, a `setState`/`Future` misuse) caught by this phase's own new tests and fixed, not shipped. **Still genuinely open:** most of the real fetchers this phase wired reachability for are not connected to live data yet — the real login screen now exists (`DEC-105`), Trust/`/trust` + Trust Digest/`/trust_digest`, Career Pipeline/`/career_pipeline`, and Finance/`/finance/subscriptions` are all real, live, and now genuinely reachable on screen (`DEC-106`, `DEC-108`, `DEC-109`), and Tasks/`/tasks` is real and live but not yet visibly reachable (`DEC-107`, blocked on Today's own unbuilt fetcher) — every other domain still needs its own live REST endpoint (Part C-2, mostly unbuilt). **Phase 1 (Sprint 0 benchmark) has a real, final result.** Real harness code, its scoring/decision logic live-verified (10/10 real tests, isolated-package verification, see `DEC-105`'s NDK/toolchain notes), the real Android NDK (`28.2.13676358`) installed, and the real on-device app run to completion on the real emulator: **both candidate models (Gemma 4 E4B, Llama 3.2 3B) genuinely failed to load**, the real, honest reason finally captured (after adding per-candidate diagnostics mid-session): `SocketException: Failed host lookup: 'huggingface.co'` -- the recurring emulator-level DNS relay failure logged across several earlier attempts in this same entry's history was the real, root cause throughout, confirmed via a real screenshot of the app's own on-device failure screen. Per `IMPL_00`'s own decision rule, `decideWinner()`'s real `StateError` escalation path fires as designed when both candidates fail -- SmolLM2-1.7B becomes the real, honest fallback primary. **Closed (`DEC-111`):** the real result is applied to `QUORUM_CONFIGURATION_CONSTANTS.md` §7 and `QUORUM_MASTER_REFERENCE.md` §5/§7, and `sprint0/` is committed and merged, on its own branch, reviewed. Phase 6 (CI hardening) remains untouched.
15. **Real, disclosed, low-probability atomicity gap in `security/supabase_deletion_store.py` (`DEC-113`, found by CRITICAL-tier review):** `purge_postgres_rows()`'s own transaction does not extend to `purge_vector_embeddings()` — a postgres-purge success followed by a vector-purge failure permanently orphans that user's `note_embeddings` (a retry 404s once the `users` row is already gone, since `_resolve_internal_user_id_or_404` can no longer find it). Deliberately not folded into one shared transaction, since both methods are independently callable and independently tested by design, matching `DeletionResult`'s own real four-store shape — forcing a merge would be architecture beyond that session's real scope (Rule 3). Disclosed directly in the module's own docstring; no fix planned unless a real failure is ever observed.
16. **No widget test exercises `you_screen.dart`'s delete-button error path end-to-end** (`DEC-113`, found by CRITICAL-tier review) — tap → catch → visible error is only structurally verified via a clean `flutter analyze`, not a real widget test. Mirrors a pre-existing, identical gap in `login_screen.dart`, not new to this branch, but worth a future widget test given this is CRITICAL-tier, irreversible-action-adjacent code.
17. **The credentials `DEC-100` printed in plaintext remain unrotated** (`DEC-114`) — `JWT_SIGNING_KEY` itself was rotated on the live deployment as of `DEC-114` (real, verified live), but `SUPABASE_SERVICE_KEY`, `UPSTASH_REDIS_REST_TOKEN`, `GEMINI_API_KEY`, `GROQ_API_KEY`, `TAVILY_API_KEY`, `GOOGLE_OAUTH_CLIENT_SECRET`, and both `LANGFUSE_*` keys were not — none are rotatable from this environment (no `supabase`/`upstash` CLI installed here, no browser automation for their dashboards or Google Cloud Console's own OAuth-credentials UI). Genuinely open, requires Preethish's own manual action in each provider's console. **`specs/tier3_verification/CREDENTIAL_ROTATION_CHECKLIST.md` (new) has the real, precise, ready-to-execute per-provider steps** (console URLs, the exact real `gcloud` command each one needs afterward -- Secret-Manager-backed vs. plain-env-var differ genuinely in what that command is -- and a real verification step per credential), prepared but not run. **A real, disclosed reason to prioritize `GROQ_API_KEY`/`TAVILY_API_KEY`/`SUPABASE_SERVICE_KEY`/`UPSTASH_REDIS_REST_TOKEN`/both `LANGFUSE_*` keys specifically, found while writing that checklist, not just prudent hygiene:** a `gcloud run services describe` call meant to check env var *names* instead printed their real, live *values* into this session's own terminal output and local transcript -- nothing was sent externally and no one else saw it, but it's a real, concrete reason those six are now higher-priority than a routine rotation sweep would suggest. No evidence any of them have actually been misused otherwise; rotating the rest remains prudent hygiene, not a confirmed incident. **A real, distinct, related gap found and closed as of `DEC-159`:** this item is about rotating credential VALUES; a separate, previously-undisclosed gap was where those values LIVED -- every real secret on the live Cloud Run service was a plain env var, never a Secret Manager reference, contradicting `infra/cloud_run/service.yaml.template`'s own long-standing documented intent. Fixed for the 6 highest-blast-radius real secrets (`SUPABASE_URL`, `JWT_SIGNING_KEY`, `GOOGLE_TOKEN_ENCRYPTION_KEY`, `GOOGLE_OAUTH_CLIENT_SECRET`, `INTERNAL_DRAIN_SECRET`, `GEMINI_API_KEY`) -- Secret Manager's own real free tier (6 active versions/month, confirmed live) made migrating all 14 real credentials genuinely impossible without incurring a real, explicitly-forbidden cost. `GROQ_API_KEY`/`TAVILY_API_KEY`/`UPSTASH_REDIS_REST_TOKEN`/`LANGFUSE_SECRET_KEY` remain real plain env vars, disclosed rather than silently left. This item (rotation) and `DEC-159` (storage location) are genuinely separate axes -- closing one doesn't close the other, and rotation of the now-Secret-Manager-backed values remains exactly as open as before.
18. **A real, disclosed, Preethish-approved deviation from a "Locked" spec decision (`DEC-120`):** `QUORUM_MASTER_REFERENCE.md` §5 locks the embedding model as Qwen3-Embedding-0.6B. That model was never wired anywhere in this backend and its intended runtime was never specified; running a real ~0.6B model on serverless, scale-to-zero, free-tier Cloud Run is a genuine cold-start/resource risk, and `note_embeddings` stores raw `content` in Postgres anyway so on-device generation buys no real privacy here. **Gemini's embedding API (`gemini-embedding-001`) is what actually runs.** Logged here per Rule 3 rather than absorbed silently into code. This also closes `QUORUM_MASTER_REFERENCE.md`'s own open item #5 (unconfirmed embedding dimension): the real, live-confirmed answer is **768**, deliberately not the model's own 3072 default, because pgvector refuses to index above 2000 dimensions — confirmed live. `SearchItemType` also gained an `application` value, a real, disclosed extension of an enum that was already documented as a reasoned construction, never a literal spec value.
19. **`migrations/0005_search_embeddings/down.sql` is a development-time convenience, not a real production rollback path** (`DEC-120`) — confirmed live that it fails **loud** (never silently truncating) once real 768-dimension rows exist. Reverting for real after embeddings exist means accepting the loss of every stored vector; they would all need regenerating from `content` regardless, since the original `VECTOR(1024)` was never a dimension any real model here produced.
20. **Unified Search's lazy backfill can return honestly incomplete first results** (`DEC-120`) — a real, deliberate trade-off, disclosed rather than hidden. This backend has no write path that creates a task/expense/application, so there is no "on creation" moment to embed at; `search()` backfills inline, capped at a real `MAX_BACKFILL_PER_REQUEST = 50` because at Cloud Run `--concurrency=1` an unbounded backfill would block a whole instance and could exceed the request timeout entirely. Self-healing across calls. A `pg_cron`-driven pre-emptive backfill was considered and genuinely deferred as separate, larger scope.
21. **Genuinely fluctuating, not a clean resolve — corrected here after `DEC-125` briefly, wrongly, called this "RESOLVED."** `gemini-3.6-flash`'s free-tier quota (a real, live-confirmed limit of 20 requests) was confirmed exhausted across at least two separate real calendar days (`DEC-121`, refined `DEC-123`), including twice more early in `DEC-125`'s own session. Later in that same session, a full-suite `pytest` run found `test_gate_llm_calls.py`'s own live Judge test genuinely passing — one real, successful live call — and `DEC-125` generalized that single success to "quota resolved." **`DEC-126` re-tested directly and found `test_negotiation_gemini_calls.py`'s three live tests still genuinely failing with the same live `429`, in the same session the Judge call itself had been passing** — proof this is not a simple on/off daily gate: different real calls against the same key, close together in real time, can land on opposite sides of it. The most accurate real statement is: this quota is genuinely live, fluctuating, and not fully understood client-side — a passing call at one moment says nothing about the next. **Do not generalize a single success to "resolved" again** — retry the specific test that matters, don't infer from a different one. Only one retry attempt was made this session, per the standing "repeated retries may be counterproductive" discipline (`DEC-123`). Lighter models (`gemini-flash-lite-latest`, `gemini-3.1-flash-lite`) have separate, currently-available quota, confirmed live — not switched to, since that would silently downgrade the actual Generator/Judge model's real output quality, a decision left to Preethish rather than made unilaterally to route around a testing inconvenience. **A further real, live data point (`DEC-127`):** a real, full-suite `pytest` run later the same session found every real, live Gemini-dependent test in the entire backend genuinely passing at once, `test_negotiation_gemini_calls.py`'s three tracked failures included. Consistent with "fluctuating," not evidence of a new steady state — a clean run doesn't mean the next one will be.
22. **RESOLVED, `DEC-122`.** The real demo dataset (tracked since `DEC-059`) is real and live — `scripts/seed_demo_dataset.py`, seeded under the one real (non-test) account this database has, confirmed on the real physical device across Today/Tasks/Career Pipeline/Subscriptions/Search. The one remaining piece — the seeded negotiation's real `positions`/`options` — stays genuinely deferred, blocked on open item #21's quota; rerun `--with-negotiation-detail` alone (no `--force`) once it resets. A real, disclosed mistake happened and was fixed the same session: an early version needed `--force` just to retry that one step, which genuinely duplicated every seeded row (confirmed live, cleaned up by hand) — fixed at the design level so `--with-negotiation-detail` alone can never duplicate anything again.
23. **RESOLVED, `DEC-123`.** `POST /negotiations/{negotiation_id}/choose` is real and live — real per-user scoping, real typed error handling (`404`/`409`/`400`), a real enqueue into `retry_queue` (a genuinely existing, previously-unused table whose own name and shape match the spec's "enqueued" language literally). A real, disclosed remainder: no `retry_queue` drainer exists yet to actually process a queued job — see item #26.
24. **`fetchGateReveal` remains unwired in `main.dart`** (noticed `DEC-122`, real and pre-existing, not new) — the real, seeded "Needs you now" card renders correctly but isn't yet tappable into a Gate-reveal detail view. **A real, disclosed scope-widening finding, `DEC-126`:** this is not a small client-wiring gap like `DEC-121`'s negotiation-detail item was. `main_shell.dart`'s own `GateRevealFetcher`/`_GateRevealLoader`/navigation are already real and complete (`DEC-104`) — only the actual fetcher was ever missing. But `GateRevealBundle` needs real `findings`/`objections` for a given `proposal_id`, and confirmed directly against the real schema (`migrations/0001_initial_schema/up.sql`): `action_events` has `gate_decision`/`outcome` summary columns only — no `findings`/`objections` persistence anywhere in this database, for any row, seeded or real. No backend route exists either (confirmed by grep against `main.py`). Genuinely wiring this needs: a schema change (new columns or table), a real code path that populates them (which itself needs `gate.review()` invoked against real production actions — the same "no production trigger" gap `DEC-119`/`120`/`121`/`125` each separately disclosed), a new endpoint, and only then the mobile fetcher. Not built this session — presented to Preethish as its own real scope decision rather than either fabricating placeholder findings/objections (forbidden, Rule 1) or silently expanding a "quick cleanup" item into a multi-part feature.
25. **RESOLVED, `DEC-126`.** The four orphaned `test-`-prefixed rows (noticed `DEC-122`) are gone — deleted via the real, already-tested `delete_account()` path (not raw SQL), one call per real `google_sub`, each confirmed to genuinely own exactly one real Postgres row (`postgres_rows=1`, itself) before deletion and zero afterward. `SELECT ... WHERE google_sub LIKE 'test-%'` on the live `users` table now returns zero rows.
26. **RESOLVED, `DEC-127`.** The real `retry_queue` drainer now exists — `features/retry_queue_drainer.py` + `negotiation/downstream_translation.py`, exposed via a real, live `POST /internal/drain-retry-queue` (a real, static shared-secret auth, not a user Bearer token). Real translation (Gemini) into one of the 3 real negotiation-eligible domains' (`finance`/`tasks`/`calendar`) structured proposal arguments, real Stage A (`provenance_check` always, `deadline_conflict_check` for `tasks`), real Stage B (`DEC-125`'s Critic/Judge), and a real, persisted `action_events` row per downstream action. **Two real, disclosed, deliberate scope boundaries remain, not oversights:** (1) this drainer produces a real Gate verdict and stops — it does not execute the approved action's real-world effect; no execution layer exists anywhere in this backend for any action, confirmed by direct search before this session. (2) `pg_cron`/`pg_net`, confirmed live NOT enabled on the real Supabase project, remain a real, manual, Preethish-only action via the Supabase dashboard — `backend/scripts/enable_retry_queue_drain_cron.sql` has the real, ready-to-run, genuinely-unverified-live SQL for once that's done.
27. **RESOLVED, `DEC-124`.** `DELETE /account` now genuinely purges `action_events`/`negotiations` (found and disclosed `DEC-123`, closed as its own explicit CRITICAL-tier session per that entry's own stated intent, not an incidental patch). `purge_postgres_rows()` deletes both inside the same real transaction as `tasks`/`expenses`/`applications`/`interviews`/`users` — confirmed neither table is ever a real foreign-key target before adding them, so ordering doesn't matter. Verified at both the store layer and the real `DELETE /account` HTTP layer, cross-user isolation proven for both tables specifically, not inferred from the `tasks` case. `retry_queue` remains genuinely, permanently out of scope — no per-user ownership column exists on it to scope a bulk delete against, a real, permanent limitation, not a gap a future session should expect to close.
28. **PARTIALLY RESOLVED, `DEC-128`, `DEC-142`, `DEC-148`, `DEC-151`.** `features/action_executor.py` is the real execution layer, now covering seven real action types: `CREATE_TASK`/`LOG_EXPENSE` (`DEC-128`), `SEND_EMAIL`/`ARCHIVE_EMAIL`/`LABEL_EMAIL` (`DEC-142`, real Gmail API calls), `UPDATE_BUDGET` (`DEC-148`, a real, direct `users.monthly_budget_limit` write, migration `0015`), and `CREATE_CALENDAR_EVENT_EXTERNAL` (`DEC-151`, a real Google Calendar API call, live-verified create→get→delete). **Confirmed by direct check against `router.STAKES_TABLE` before relying on it:** `CREATE_TASK`/`LOG_EXPENSE` are real `Stakes.S1` -- Stage B never runs, so the payload executed is always the original, already-validated one, never an unvalidated Judge revision. **`UPDATE_BUDGET`/`CREATE_CALENDAR_EVENT_EXTERNAL` are genuinely different, weaker cases, a real CRITICAL-tier review finding (`DEC-148`, BLOCKER):** they are real `Stakes.S2`/`S3` respectively, so the real Judge DOES run for both, and a Judge `decision="revise"` can become a final `approve` carrying that Judge's own `revised_payload` -- a real `dict` with no schema guarantee beyond that. Each branch is therefore its own real, independent last line of defense, not a beneficiary of the S1 guarantee: `UPDATE_BUDGET` rejects a non-finite (`NaN`/`inf`), non-positive, or implausibly large real ceiling before any write; `CREATE_CALENDAR_EVENT_EXTERNAL` rejects a malformed title/email/non-tz-aware/inverted-range datetime before any network call and sends Google's real `sendUpdates=all` parameter (a real CRITICAL-tier review HIGH finding, `DEC-151` — omitting it would have silently booked an event while never notifying the invitee). No header- or path-injection guard was needed for its fields (unlike `SEND_EMAIL`/Gmail-id fields, since none of them ever reach anywhere but a JSON body) — but a real, disclosed residual risk remains and is held only by the S3 human-approval backstop, not by format checking: a Judge-revised payload could still carry a syntactically valid but attacker-chosen `invitee_email`/`title`. A real, hand-verified fact narrows where the `UPDATE_BUDGET` check actually bites today: Postgres's own `jsonb` type genuinely rejects the literal `NaN`/`Infinity` JSON tokens first, at `_persist_verdict()`'s own `INSERT INTO action_events` (confirmed live against the real Supabase database) -- so a `NaN`/`inf` `revised_payload` fails the job earlier and never reaches this branch through today's one real path; the branch's own check is what actually catches a finite-but-implausibly-large real ceiling, which sails past Postgres's jsonb parser untouched and would otherwise produce a division-by-zero or an inverted-sign fraction in every real per-user computation that divides by it. It also verifies the real `UPDATE`'s own row-count tag before reporting success, closing a second real review finding (HIGH) where a nonexistent `user_id` would otherwise silently report `executed=True` for a write that never happened. **Still genuinely, deliberately out of scope, exhaustively enumerated (all 4 real remaining `ActionType`s), not silently skipped:** `CREATE_CALENDAR_EVENT_LOCAL` (real local-event ground truth belongs on-device, not a server-side table), `UPDATE_TASK`/`UPDATE_APPLICATION_STATUS` (never produced by any real code path that reaches this function), and `CREATE_NOTE` (no real `notes` table — only `note_embeddings`, a genuinely different concept). Every one of these returns a real, honest, non-executing result naming why, proven by an exhaustive test iterating every real `ActionType` other than the seven executable ones. **A real, disclosed caller gap for `CREATE_CALENDAR_EVENT_EXTERNAL`, matching `SEND_EMAIL`'s own exact precedent:** `retry_queue_drainer.py`'s own calendar domain translation always sets `has_external_invitee=False` (a negotiation option's free text never names a real external attendee's email, and guessing one would be fabrication), so no real code path in this backend produces this proposal today — real, live-verified execution capability with no real caller yet. **Two further real, live-found findings from `DEC-128`'s own CRITICAL-tier review, both fixed before merge:** an unbounded, LLM-supplied `estimated_hours`/`amount` could previously reach a fixed-precision `NUMERIC` column it doesn't fit (now real, upper-bound-checked before translation, matching each column's real precision); and a genuine DB-level failure mid-job could previously leave `_mark_job_failed()`'s own recovery `UPDATE` running against an already-aborted transaction, permanently wedging that job at `attempt_count=0` instead of genuinely backing off (fixed structurally — the failure now rolls back cleanly before recovery runs, as its own separate statement). **A real, disclosed schema gap found and closed in the same session (`DEC-128`):** `expenses.source`'s real, closed 3-value `CHECK` (`on_device`/`manual`/`extracted`) had no value honestly describing a Gate-executed row — migration `0007` (new) adds a real, precisely-named fourth value, `gate_approved`, applied live to the real Supabase database. **A second real, disclosed gap found, not silently worked around:** the real `expenses` table has no `category` column at all — a real, translated category survives in `action_events.payload` but is genuinely not persisted onto the `expenses` row itself. `UPDATE_BUDGET`'s own real payload has the identical, disclosed pattern — `payload["category"]` is genuinely not persisted either, since this is a single, whole-account ceiling, not a per-category one. `CREATE_CALENDAR_EVENT_LOCAL` and the `notes` schema gap remain genuinely open, tracked as their own future scope, not this item's to close.
29. **RESOLVED, stale fact corrected here by a direct, live re-check this session (previously claimed `pg_cron`/`pg_net` were NOT enabled, found `DEC-127`).** `SELECT extname FROM pg_extension` on the real, live Supabase database now returns `pg_cron`/`pg_net` alongside `vector` and the rest — both were enabled at some point between `DEC-127` and `DEC-134` (the exact session isn't recorded in this log; `DEC-134`'s own text was the first to describe a job as "real, live, on a real schedule," a real, prior, undisclosed gap in this project's own logging discipline, not a mystery worth chasing further now). **Five real `cron.job` rows are live and active** (`drain-retry-queue` `*/5 * * * *`, `deadline-watch`/`spend-alert` `*/30 * * * *`, `backfill-negotiation-detail` two-per-hour, `email-ingestion` four-per-hour) — `career-digest` is deliberately not among them yet, see `enable_career_digest_cron.sql`'s own top comment. **A real, direct query of `cron.job_run_details` this session finally, definitively answers the long-standing "does pg_cron's own firing prevent Supabase's free-tier inactivity pause" question, referenced elsewhere in this project's history as still-open:** all five jobs have been running continuously since 2026-08-25, **zero failures across 5,549 total real runs** (`drain-retry-queue` alone: exactly 288/day, every day, matching its 5-minute cadence with no gaps at all) — 10+ real, unbroken days of activity is strong, real, live evidence the answer is **yes**, pg_cron's own regular activity keeps this free-tier project from pausing. (`return_message`/`status` come from `pg_net`'s own async HTTP call succeeding, not from a job's own internal `users_failed` tally -- a job can show `succeeded` here while still honestly tallying individual per-user failures internally, a genuinely different, already-covered layer of failure isolation.)
30. **Phase 2 of `QUORUM_PRODUCTION_COMPLETION_PLAN.md` is partially real, `DEC-132`/`DEC-133`.** Two of the plan's four named jobs are now real: `POST /internal/deadline-watch` (`features/deadline_watch.py`) and `POST /internal/spend-alert` (`features/spend_alert.py`), both genuinely autonomous, non-manual callers of `negotiation/trigger.py::scan_for_conflicts`, both real and tested, both **CRITICAL-tier reviewed** (deadline-watch cross-model, Opus, `DEC-132`, which found and fixed two real, severe, live-proven bugs before merge — see that entry's own full account; spend-alert's own review dispatched, `DEC-133`, update this line once it returns). Both create a real, *bare* negotiation row only (no Gemini-backed positions/options — a real, disclosed, deliberate scope boundary against this project's already-fluctuating Gemini quota). `features/negotiation_trigger_support.py` (new, `DEC-133`) factors out every real primitive both jobs share, including a real, precise, `trigger_source`-scoped idempotency check (migration `0008`, applied live) that keeps the two jobs' own detections from suppressing each other. Still genuinely open within this same phase: the other two named jobs (briefing, follow-up); real detail-generation for an autonomously-created negotiation (the real, complete fix for the bare-negotiation cooldown mitigation, not just a patch); a real, disclosed, pre-existing plaintext-secret gap now in all three `/internal/*` cron-enable scripts (Supabase Vault, not attempted, new scope); and — blocked on item #29 above — the real, end-to-end, on-device proof (sign in, let a real cron interval pass, see a real negotiation appear in Today's "In motion" with nobody having run a script).

31. **The real Cloud Run runtime service account holds project-wide `roles/editor`, broader than the real secret-access it now needs** (found `DEC-159`, CRITICAL-tier review, informational -- pre-existing, not introduced by that session). `649581407643-compute@developer.gserviceaccount.com` is the GCP-assigned default compute service account, never replaced with a dedicated, narrowly-scoped one. The review confirmed the per-secret `secretAccessor` grants added in `DEC-159` are genuinely load-bearing (`roles/editor` does NOT itself include `secretmanager.versions.access`) -- but `roles/editor` DOES include `secretmanager.versions.add`/`versions.destroy`/`secrets.delete`, so a compromised runtime could still add a version of a secret it can read, or destroy secrets outright (an availability risk), independent of the new access grants. A dedicated runtime service account without `roles/editor` would be the real fix -- out of scope for `DEC-159` itself (a pure secrets-storage migration), logged here as its own real, disclosed follow-up rather than silently left unexamined once found. **A real, materially larger exposure surfaced by `DEC-160`'s own CRITICAL-tier review:** this same identity is also the real, confirmed execution identity for every Cloud Build job this project now runs automatically (`DEC-160`'s new CI/CD pipeline) -- confirmed live via `gcloud builds describe` on several recent builds. This means a Dockerfile or build-step change merged to `main` now auto-executes with project-wide Editor rights, unattended, with no human at the keyboard, materially raising the real stakes of this same pre-existing gap beyond its original "compromised runtime" framing. The real fix (`gcloud builds submit --service-account=<a dedicated build SA>`) remains out of scope for `DEC-160` itself (which was already, correctly, scoped to real IAM discovery for the CI deployer identity, not a redesign of Cloud Build's own execution identity) -- still logged here as the same standing, real, disclosed follow-up, now with its actual blast radius stated accurately. **The Cloud Build half is now RESOLVED (`DEC-162`), independently CRITICAL-tier reviewed with no blocking findings:** a new, real, dedicated `quorum-cloud-builder` service account holds `roles/cloudbuild.builds.builder` at the project level (verified: no other project-level role) plus one additional, real, bucket-scoped `roles/storage.objectAdmin` grant on `gs://quorum-505909_cloudbuild` for reading source/writing logs -- the review found this bucket grant genuinely redundant with permissions `cloudbuild.builds.builder` already provides project-wide, and broader than strictly needed (adds `setIamPolicy`/`move`/`retention`-family permissions unused by this pipeline); narrowing it to `roles/storage.objectCreator` (verified safe with one throwaway build first) is a real, disclosed, deliberately-deferred follow-up, not done in the same unsupervised stretch that created it. **Fully verified, RESOLVED:** one real, manually-impersonated test build first proved the identity works (`gcloud builds describe`'s own `serviceAccount` field), and the real, first GitHub-Actions-triggered build after this PR's own merge (`519c574b...`, 2026-09-05) independently confirmed the CI/OIDC identity (`quorum-ci-deployer`) genuinely exercises the new flags too -- contrasted live against the immediately-prior real build, which still ran under the old default compute identity, a clean before/after; the deploy that followed served a real `200` from `/health` afterward. **Residual real risk, disclosed rather than treated as fully closed:** `roles/cloudbuild.builds.builder` still includes project-wide `storage.objects.delete` and `artifactregistry.repositories.deleteArtifacts` -- a compromised build step could still delete real production images/objects, though it can no longer deploy Cloud Run, read the 6 migrated secrets, or impersonate any service account (all real, confirmed absent from this role). **The Cloud Run RUNTIME half remains genuinely, deliberately open** -- `649581407643-compute@developer.gserviceaccount.com` is still the live, traffic-serving service's own execution identity, still holding project-wide `roles/editor`. Re-pointing the actual live application at a new identity is real, higher-risk, harder-to-reverse work (re-granting every real Secret Manager access `DEC-159` already gave the current identity, then re-verifying the entire live app end-to-end under the new one) -- deliberately not attempted during the same unsupervised stretch that closed the build half, and a real candidate for a future session run with Preethish available. **A real, disclosed nuance from `DEC-162`'s own review, sharpening rather than undercutting the build-half fix:** `roles/editor` includes `iam.serviceAccounts.actAs`, so this still-open runtime identity could itself submit a real build (as itself, or by impersonating `quorum-cloud-builder`) -- `DEC-162` closes the *unattended CI pipeline's* execution identity, not every real path to a privileged build.

## Update protocol

Rewritten — not appended to — at the end of every single session, per its own stated discipline. Never let this file describe a session as done without a real, live-verified reason to believe so, and never let it restate the specification narrative's claims about a codebase this repository doesn't actually contain.

---

*Read this file first, every session, before anything else — including before `CLAUDE.md`. If it disagrees with `DECISIONS_LOG.md`'s entries before DEC-050, this file is correct — see DEC-050 for why.*
