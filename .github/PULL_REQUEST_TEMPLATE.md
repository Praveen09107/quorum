## Session document

Which `IMPL_XX`/`MOBILE_XX` document does this implement? (link it)

## Real verification performed

- [ ] `ruff check backend` clean (backend changes)
- [ ] `pytest backend/tests -q` — paste real output, not "passes"
- [ ] `dart test` / `flutter analyze` (mobile changes, real machine)
- [ ] `STATUS_INDEX.md` updated
- [ ] `DECISIONS_LOG.md` entry appended, if this session found or
      fixed anything worth recording

## Review tier

STANDARD / CRITICAL — state which, and why if borderline.